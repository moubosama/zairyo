# ZAIRYO - 資材拾いアシスタント

## プロジェクト概要

ZAIRYOは、工務店向けのリノベーション資材拾い自動化システムです。
AI（Gemini + Claude API）による図面解析で、資材リスト作成を大幅に効率化します。

**検証済み精度（2026-07時点・意匠図2タイプでeval実施）**: 部屋面積±1%以内、壁面積-1.7%、天井面積+2.3%（プロの手拾い比）。詳細は `backend-node/scripts/` のeval結果JSONを参照。

**2026-07-08 Gタイプ本番再検証（専有面積入力なし）**: 専有面積0%、天井+2.2%、天井PB+4%、際根太-1%、遮音壁PB 0%、グラスウール-3%。残る誤差はラベルの無い区画（廊下・キッチン・水回り）の目測ブレ（フローリング+13%、間仕切壁延長は回により80〜110m）。専有面積を入力する通常運用なら実用精度。この検証で修正したバグ2件: パウダールームの水回り判定漏れ、外形寸法誤読による専有面積の上書き。

## 🚧 進行中タスク: 3図面入力対応（2026-07-10 第1段実装済み）

ユーザー方針: 読み取らせる図面を**平面詳細図・展開図・建具表の3枚**にし、ロジック計算する。

**アルファAタイプのブラウザ実測＝モデル比較の決着（2026-07-25 21時・gemini-3.1-pro-preview・別セッション記録）**:
とまる実験素材（平面+展開+建具表・専有71.90入力・**建物種別は未選択**）を本番3.1-pro-previewで実読み。壁PB57枚/天井46枚。
- **3.1 Proでも展開図の過大読みは直らない**: 壁PB面積が専有の**3.17倍**で実測却下（2.5-flash時2.88倍→むしろ悪化）。
  3.5-flash(符号ズレ+70%)・3.1-pro-preview(3.17x)の2実測で**「上位モデル化は解でない」が確定**。コスト増・速度低下のみ。
  → **RenderのGEMINI_MODEL（現在gemini-3.1-pro-preview）は削除して2.5-flashへ戻すこと（要ユーザー操作・未実施）**
- 57枚の直因は**建物種別未選択**（既定リノベ式0.822枚/㎡）。075c79aのUI配線はデプロイ済みのため、
  結果画面「計算条件の調整」→建物種別=新築→再計算（**AI費ゼロ**）で80枚(-5.1%)になるはず＝**次回ブラウザで要確認**
- 「積算118/61」を枚数と誤認する単位罠がこの会話でも再発（正=壁84.3枚/天井42.07枚・㎡÷1.4/1.45）。既存記録の罠が実際に効いた
- ※0a9d59d(07-25 20:46)時点で**全サイクルはコミット/push済み・作業ツリークリーン**。以下ブロック群の「未コミット」表記は陳腐化

**別府タイプ別シートの部屋ブロック正解を構造化（2026-07-26・coder/reviewer分離・承認済み・push済み）**:
別府XLSは**タイプ別シートＡ〜Ｉが全9タイプ記入済み**（各3,100〜3,600セル・Ｊ=共用部。アルファはＡ(中身G)以外未記入だったのと決定的に違う）
＝全タイプの「部屋×面×部位」正解内訳が存在する。アルファGを+3%に仕上げた武器（部屋ブロック突合＝残差の部屋単位分解）が別府9タイプ全部で使える。
- **新規 `scripts/beppu-room-truth.json`**（575KB・9タイプ592ブロック・壁1本単位のw/h/面積+控除ded:true+P小計）+
  **`eval-beppu-rooms.mjs`**（内部整合w×h=area・小計=Σ加算−控除、外部整合Σ部屋小計=集計表戸当）＝**1845✅/✗0（90セル全て0.00%）**
- **抽出の堅牢設計**: 行種別はオフセットでなくP列数式で機械判別（乗算行/直入行/小計行。ブロック周期54行±の不均一に対応）。
  外部整合マッピングはラベル突合でなく**集計表C列の参照Pセルをパース**（行ラベルズレの罠を構造的に回避・9タイプ同一をassert）。
  0スキップ行はskippedリストで検証可能化（黙った脱落はC1/C2で赤）。reviewerがXLS原典と抜き取り照合（3ブロック+linear型）全一致・
  循環検証でないこと（比較先は独立検証済みground-truth）・フォールト注入2種検出を独立確認
- **診断で判明した2つの重要事実（報告のみ・フィルタ未修正）**:
  ①**「水回りは戸境壁を持ち得ない」の前提は部分的に誤り**: 便所・洗面所は全9タイプで「遮音壁**耐水**PB張」を実在保有（10〜21㎡/戸・集計表r60=P332+P386）。
  ただしこの部位はエンジンに出力行が未実装（けいとさん保留案件）＝NON_PARTY棄却は「遮音壁PB(t9.5+GW)行の保護」としては正しい
  （正解側でも水回りにt9.5+GW行はゼロ）が、実在する耐水系戸境壁の受け皿が無いまま検出ごと捨てている構図
  ②**玄関・台所の遮音壁PB(t9.5+GW)は全タイプで実在**（A玄関13.48㎡等）**がエンジンは系統的に取り漏れ**＝A遮音-26%過少の内訳が部屋単位で特定可能に
- 抽出スクリプトは`scripts/extract-beppu-room-truth.mjs`に参照保存（xlsx依存のためリポジトリでは直接実行不可・再現はxlsx入り環境で。依存追加は禁止のまま）
- reviewer繰越nit: G玄関の際根太-13.7m（部屋跨ぎ控除の記帳流儀・物理量でない）/ 対象外部位は内部整合のみ / C2の二重改竄は±1%tol内なら理論上可（現データ非該当）
- 検証: eval-beppu-rooms 1845✅ / eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ / eval-alpha-af 567✅ / replay-gtype 10✅ / replay-beppu 6✅ / test 25件OK

**レバー1検証: PDFテキスト層からの寸法直接抽出の実現可能性（2026-07-26・課金ゼロ・30分検証完了）**:
「どの画像でも読める」戦略のレバー1（AIに推測させず確定情報を使う）のフィージビリティをPyMuPDFで実測。
- **別府PDF（JWW→PDF・サンユニオン）: 成立✅**。寸法数字は**全角**（'２,２００'等・ASCII正規表現では見えないので注意）で
  テキスト層に座標付き実在。p36平面91種+p37展開70種=合算115種。**壁記号（Ａ〜Ｇの1文字）・建具符号（ＷＤ）・部屋名・高さ（Ｈ=２,１７０）も
  全部テキストで取れる**。ベクターパス95,186本＝図形情報も完全。正解Aタイプのセグメント幅25種中**14種が直接ヒット・残り11種は全て
  寸法チェーンの和で構成可**（例: 7350=2600+4750。座標があるので隣接チェーン合算は機械解決可能）
- **アルファPDF: 不成立✗**。全ページwords=0（文字がアウトライン化されたベクターPDF・埋め込み画像もなし）＝AI vision継続が必要
- **製品設計の帰結=ハイブリッド**: アップロード時にPDFテキスト層を自動判定→寸法テキストが取れるPDFは決定的抽出パス
  （粒度問題が構造的に消える・AIは意味付けのみ）／取れないPDF・画像はAI visionパス（従来+確認UI）。
  現行アップロードはPNG化して送る運用だが、PDF直接アップロード（対応済み形式）ならテキスト層が使える
- **次の実装ステップ（未着手・大きめ）**: 寸法チェーンの座標ベース結合（同一寸法線上の隣接判定）→壁/部屋への帰属→
  既存パイプラインへの合流（wall_finish_codes/elevations相当の生成）。決定的処理なのでテスト可能・正解突合はbeppu-room-truthで機械検証可能

**レバー1第2段: 抽出プロトタイプ＋偽陽性対照（2026-07-26・coder/reviewer分離・must-fix1往復で承認・push済み）**:
`pdf-dim-extract.py`（PyMuPDF・正解JSON非参照の構造的分離）+`verify-pdf-dims-beppu-a.mjs`+抽出結果JSON。
- **正直な現在地（見出し=直接一致のみ）**: 別府Aタイプ正解セグメント幅に対し**壁PB直接50.0%（10/20）・遮音PB直接77.8%（7/9）・合算57.7%**。
  50mm格子ベースレート32.4%を有意に上回る本物の信号（p≈0.003級・reviewer二項検定）
- **【教訓】チェーン和込み76.9%は偽陽性支配で証拠にならない**: reviewer対照実験で50mm刻み乱数が90.8%ヒット
  （建築寸法の格子性でチェーン和747種が50mm格子をほぼ飽和）＝実測が乱数対照を下回る指標だった。見出しを直接一致に是正し
  ベースレート対照を出力に常設（**次段の芯々−壁厚差し引き層では候補が壁厚4種で4倍に膨らむため同種の対照併記が必須**と申し送り）
- **構造の発見**: 直接ヒット15種は全て50mm格子上・未構成6値は全てオフ格子で「**芯々−壁厚（60/110/120/220）**」の差で構成可
  （1880=2000−120等）＝**テキスト層は芯々寸法・XLS正解は内法幅**という関係がデータで裏付け。次の本丸=壁厚の帰属ロジック
- p37展開図はテキスト層に壁記号・建具符号なし（p36平面に壁記号68件[遮3含む]・ＷＤ符号10件あり）＝記号は平面から・幅は両ページからが筋
- 検証: 抽出は決定的（2回実行バイト一致・reviewer確認）・test 26件OK・エンジン非接触

**レバー1第3段: 壁厚変換と部屋帰属の測定（2026-07-26・coder/reviewer分離・承認済み・push済み）**:
`verify-pdf-dims-beppu-a-lever3.mjs`新規＋抽出器拡張（壁厚候補の機械導出=頻度≥5 in[50,300]+凡例t=NNN・部屋帰属用bbox）。
- **【負の結果・正直な報告】機械導出壁厚での芯々→内法変換は「効かない」**: ヒットは15→18/26に増えるが
  シグナル（実測−50mm格子対照）は無変換+25.3pt→V1+11.5pt/V1p+16.4ptに**希釈**＝候補集合の膨張（115→425/761種）が
  ベースレートを先に押し上げ、増えたヒットの過半は偶然（前サイクルmust-fixの対照併記が機能した実例）
- **真の欠落オフセットはt=120と診断**（未一致[1530,1580,1880,2430]が一斉に閉じる・3430=3550−120）。
  t=120は図面テキストに頻度1で機械導出不能。**凡例t100+PB9.5×2≒119の「仕上厚合成」仮説**は立つが採用は答え合わせと
  紙一重＝未採用（診断報告のみ・次の設計判断）。reviewerがt=120の隠れ採用ゼロ・閾値5は谷（頻度4〜6同一集合）で
  微調整余地が構造的に無いことを裏取り
- **部屋帰属は7室中4室で有効**（玄関+5.8/洋室3+20.4/便所+17.6/洗面+18.3pt）・3室で負（LDK-11.7/洋室1-31.0/洋室2-10.3
  ＝寸法線が部屋外側に引き出されラベル近傍窓で掴めない）。7350（LDK最大幅）は直接寸法に不存在
- **戦略的帰結**: 抽出寸法は精度~100%（実在寸法）・網羅50〜78%。「AIの置換」より先に**「AI読みの検証アンカー」**
  （AIが読んだ幅が抽出集合に無ければ警告/棄却＝遮@9700級の幻覚を機械検出）としての実戦投入が有望＝レバー2と合流
- reviewer繰越nit: 50mm格子対照は変換集合で過小評価側（結論「効かない」はむしろ強まる方向・次段で10mm格子併記）/
  V2ラベル「飽和対照」は不正確 / V2寄与はt=120に加えt=70も / 閾値コメントはp36単独分布を引用（適用は合算・谷は合算でも成立）

**レバー1第4段: t=120合成則は「効く」（2026-07-26・coder/reviewer分離・承認済み・push済み）**:
`verify-pdf-dims-beppu-a-lever4.mjs`。凡例「t=100 PBt9.5二重貼…」から総厚=100+9.5×2=119→round10=**120を完全機械合成**
（数値ハードコードなし・reviewer確認）。最小候補（この1値のみ）で測定:
- **合算57.7%→73.1%・遮音PB 9/9=100%・シグナル拡大**（50mm格子+25.3→+40.0pt / 10mm格子+49.9→+58.5pt）＝
  候補1個分のベースレート上昇を明確に上回る（第3段の8厚希釈+11.5ptと対照的）。raw119は+0件＝round10（正解が10mm粒度）が必須
- **正直な限定**: 単一厚スイープではt=170(+5件)がt=120(+4件)より上位・t=70/220も同格。だが170/70/220は凡例から導出不能＝
  採用すれば正解逆算。**t=120だけが図面から先験導出できる**のが採用根拠（未一致最小化での選択はしていない・reviewer裏取り）
- 残る未一致[2580,2900,3100,3430,3550]はチェーン和−120でのみ閉じる（格子飽和で信用不可）・7350は閉じない

**レバー1第5段: 壁線幾何プロトタイプ＝内法スパンが図形から直接取れる（2026-07-26・coder/reviewer分離・承認済み・push済み）**:
`pdf-wall-geometry.py`+`verify-wall-geometry-beppu-a.mjs`。p36のベクターパス（軸平行H13,973/V15,945・斜め18,075=RCハッチング・
塗り矩形なし）から平行線ペア（間隔50〜300mm）→スキャンライン隣接ギャップ＝内法スパンを計算。
- **スケール自動推定: 1/59.99（sd0.147%）**＝テキスト寸法と図形距離の突合で縮尺1:60を機械特定（この手法自体が汎用資産）
- **最良[all/支持≥3/±10mm]: 合算84.6% vs 対照38.7%（シグナル+45.9pt）・遮音PBセグメント幅9/9=100%**。
  ±25mmなら最大96.2%（thick/支持≥1）。バリアント8組は事前定義の全組報告（一致率選別なし・reviewer確認）
- **全バリアント共通未一致は7350のみ**（台所A面=LDK長辺。袖壁スタブでスパン分割＝「隣接ギャップの和＋介在壁厚」の再構成が次段）
- 壁厚クラスタ（55/75/100/110/150mm）は第3段の寸法頻度由来候補と整合。RC/LGS識別（ペア間ハッチ有無）・部屋帰属は次段
- 抽出は決定的（再実行バイト同一・reviewer確認）。reviewer SF-1（ヘッダー観察数値の陳腐化）は修正済み
- **戦略的到達点**: テキスト層（芯々+t=120則=73.1%）と壁線幾何（内法直接=84.6%）の**独立2ソース**が揃い、
  相互照合＋AI読みの検証網（幻覚幅の機械検出）の部品が完成。次=部屋帰属と2ソース統合

**レバー1第6段: 値照合の検証網は「機能しない」と確定（2026-07-26・測定のみ・2026-07-27独立レビュー承認済み＝確定）**:
`verify-anchor-beppu-a.mjs`（coder実装がセッション上限で中断→ファイルは完成しておりメインループが実行・検証。
**独立レビュー実施済み（2026-07-27）: 決定性2回バイト一致・循環なし（truth参照grep陰性・classify()はD/S/spans/T_COMPOSEDのみ）・
ベースレート45.1%=格子142点中64点を検算一致・幾何パラメータ全12組合せでe=0件＝結論はパラメータ非依存**）。2ソースアンカーで別府AのAI読み幅89件を分類×正誤照合:
- **機械判定: 検出力なし＝単純値照合は検証網として機能しない**。AI読みの**98.9%が網一致**（50mm格子ベースレート45.1%+53.8pt）
  ＝AIは実在値をほぼ必ず転記している。誤読38件のうち値照合で捕まったもの**0件**＝**誤読の実質100%が帰属間違い**
  （実在する寸法値を別の壁に付けている。玄関・廊下B面4450→正解3430Δ1020等の実例37件を列挙。
  reviewer精密化: 厳密には37/38＝残り1件のLDK D面6950は直接寸法に不在でチェーン和のみ一致＝AI側の合算生成の可能性。
  37件にも純帰属間違いと合算粒度ズレ（LDK A面8200等）が混在するが「値照合では捕まらない」という操作的結論はどの機構でも同一）
- **仮説の最終確定**: 「AIの誤読は値の捏造でなく帰属間違い」が定量で証明された。よって図面抽出2ソースの使い道は
  「AI読みの事後検証」ではなく**「空間帰属つきの置換」**（壁線幾何の部屋帰属→AI読みの代わりに幾何値を使う）一択
- 逆側の損失（網不一致なのに正読=過剰棄却）も0件＝網は何も弾かないし何も守らない。感度チェック（全部位基準）でも同結論

**レバー1第7段: 展開図テキスト層の部屋別面幅抽出は「単独では否」と確定（2026-07-26・goal完了・2026-07-27独立レビュー承認済み）**:
`verify-elev-room-widths-beppu-a.mjs`（p37の部屋ラベル×寸法の座標近接帰属・測定のみ）。goal=可否の確定（高一致率の強制なし）。
- **結果（壁系部位限定の公平突合）**: 部屋帰属あり**20.0%**（8/40）vs 帰属なし52.5%（21/40=p37上の存在上限）＝**段組み帰属は上限をさらに取りこぼす**
- **原因3層**: ①構造的上限52.5%（壁幅の半分はp37に不在＝芯々−120の内法値は平面p36側・7350等は分割表記のみ）
  ②寸法が立面枠外（隣の行帯）に描かれ同一値が複数室に出現＝ラベル起点の帯割りでは曖昧 ③高さ系（CH2200/階高2960/建具高2175）の
  全区画混入（チェーン軸のインスタンス単位除去でも境界が残る）
- **測定過程**: 中点帯割り16.5%→行起点+値単位高さ除去2.4%（過修正）→インスタンス単位11.8%→公平化20.0%。全て対照つき＝否は頑健
- **戦略的帰結（役割分担の最終形）**: 幅の主力=壁線幾何（p36・84.6%/遮音100%）／テキスト層=スケール校正・t=120則・
  **部屋名/壁記号/建具符号の供給源**（ラベルは100%取れる）／AI vision=テキスト層ゼロPDF（アルファ型）と意味付け補完
- **テキスト層判定関数の仕様案（パイプライン分岐用）**: `hasDimensionTextLayer(pdf,page)`=words取得→NFKC+カンマ除去→
  /^[0-9]{3,5}$/かつ値∈[300,12000]を計数→**≥20でtrue**（実測: 別府p36=無フィルタ91種・**仕様案自身の条件[300,12000]では64種**＝
  実装時は64を基準に（reviewer nit）。いずれも閾値20の3倍超でtrue／アルファ全ページ0種false＝マージン十分）
- 検証: test 26件OK / eval-gtype 20✅⏳0✗0 / replay-beppu 6✅・src非改変（新規検証スクリプト1本のみ）
- **独立レビュー実施済み（2026-07-27・第6段とともに承認＝確定）**: 20.0%/52.5%とも再現・決定性2回バイト一致・帰属ロジックの正解非参照・
  「存在上限」枠組みの妥当性を確認。must-fix/should-fixゼロ。残nit: スクリプト表示の平均候補数分母が死にエントリ込み8室
  （実マッチ7室なら5.7種/室・絞り込み9.4倍が正）／e=0件時サマリ行にNaNpt表示（いずれも表示のみ・記録数値には非引用）
部屋単位診断で確定した原典事実に基づくスコープ修正。**数値定数の新設ゼロ**（正規表現2件=既存NON_PARTYのサブセット分割・振替式=既存遮音壁式の流用）。
- **①収納室スコープ除外**（`BEPPU_CLOSET_SCOPE_ROOM_RE`・isBeppuLayoutゲート）: 展開図に現れた収納室（SCL/WCL/パントリ/押入/物入等）を
  部屋ごとtakeoff寄与から除外（壁PB・巾木・周長・下地・開口すべて）。**正確な根拠（reviewer SF-1で訂正済み）**: XLSタイプ別シートに
  SCL/WCLブロックは**無い**・押入/物入ブロックは**実在する**（中身は洗面片引き軸組・ｼｽﾃﾑ収納床組・際根太・**EV面遮音壁（界壁）8〜23㎡**等）が
  **壁ボード・巾木の行は無い**＝「収納室の壁PB・巾木は拾わない」が正。
  【既知の限界】将来展開図に押入/物入が現れEV面記号が付くと、部屋ごと除外がEV廻り壁の実在寄与ごと落とす狭い穴（現記録はSCL/WCLのみ＝非発生・
  警告の部屋名で気づける・是正は押入/物入をREから外しEV系記号面だけ拾う分岐＝次サイクル判断）
- **②水回り遮/G枠の耐水バケット振替**（`SOUND_WET_ROOM_RE`）: NON_PARTY棄却→新バケット`sound_wall_waterproof_pb_sqm`へ
  （XLS集計表r60「遮音壁耐水ＰＢ張」=全9タイプ実在23.7〜35.2㎡/戸の受け皿）。**表示スコープ外**（資材行新設せず・summaryは検出>0時のみ・
  けいとさん保留案件と分離）。検出はA6.35/B6.20/D8.45㎡=正解の18〜28%（AIの水回り読み取り限界をそのまま受容＝水増しなし）。
  ②単独では壁PB・遮音壁PBは1バイトも動かない（棄却時も計上していなかったため）
- **効果（壁PB・㎡）**: A 98.13→**76.98（+30%→+2%✅）** / B 58.74→58.00(+75%) / C 61.99→57.07(-17%→-23%) / D 46.45→32.05(-1%→-32%)。
  **C/Dの見かけ悪化は相殺剥がれの正直化**（収納誤計上が玄関/台所の読み欠落過少と打ち消し合っていた。部屋単位では正確化・
  残る過少は読み取り欠落=AI側）。遮音壁PB・天井PBは全タイプ不変
- **アルファ不変**: reviewer独立再現（HEADのsrcをgit archive展開しアルファ3記録をフルパイプライン比較）で**バイト一致**。
  eval-gtype 20✅⏳0✗0 / replay-gtype 10✅
- 検証: eval-beppu 453✅ / eval-alpha-af 567✅ / eval-beppu-rooms 1845✅ / replay-beppu 6✅（スナップショット更新・変化理由を部位単位でコメント明記）/
  test 26ファイル全緑（新規test-beppu-scope 47✅・既存3ファイルの旧仕様アサーション4件を新仕様へ正当追随）/ フォールト注入3種検出
- reviewer繰越: SF-2 ②セグメント経路は同じ壁が壁PB行にも載りうる（経路間非対称・警告に二重手配注意を追記済み・r60行を将来実装する際の要注意）/
  SF-3 nonparty警告文の陳腐化（修正済み・到達はバルコニー/倉庫のみに）/ SF-4 coder報告漏れ2件（A耐水PB 6.31→5.65㎡=SCL/WCL内耐水面の除去・
  間仕切下地の減少 A65.4→51.8/D49.0→39.7=いずれも①の意図どおりの帰結）/ nit plan-only収納の3×√推定は従来どおり（展開図経路限定の除外）

**別府A〜D残差の部屋単位分解診断（2026-07-26・diag-beppu-rooms.mjs新設・診断のみ・コード不変）**:
beppu-room-truthを使い、壁PB/遮音PB残差をLOO差分法（全室takeoff−部屋i除外）で部屋単位に分解。フィルタ分類はエンジンプレパスのミラー+実カウンタ突合で全タイプ✅一致。
- **【最重要】Dの総量-1%✅は相殺の見かけ**: 玄関-12.66/台所-13.04㎡の過少とWCL+14.40/洋室(1)+6.64㎡の過大が打ち消し合っただけで部屋レベルでは合っていない。総量の緑を部屋分解で検証する必要性の実証
- **壁PB過大の最大源=正解に部屋ブロックが無い部屋への既定G14計上**:
  ①収納（A:SCL+17.86㎡/D:WCL+14.40/C:WCL+4.92）＝XLSは別府の収納壁を拾わない（システム収納・工事対象外）
  ②水回り（B便所+9.89等）＝**別府の便所・洗面の壁はほぼ全量「遮音壁耐水PB張」というエンジン未実装部位**（23.7〜35.2㎡/戸・全9タイプ実在・けいとさん保留案件とセット）で、通常壁PBは正解0
- **打放し既定（c6944baの安全版）が玄関・台所で過剰**: C04を読めた部屋の未割付面が全部打放しに倒れ、玄関の壁PBがA/C/Dでエンジン0㎡（正解12.7〜22.1㎡）
- **遮音PBの取り漏れ機構が確定**: 玄関はA/B/Cで遮/G枠の**検出0件=AI未読**（面幅マッチ失敗でもフィルタ棄却でもない・正解は10.4〜13.5㎡の大口）。
  台所は(i)未読(A)(ii)長大合算遮@9700(B)(iii)D面一律→face_label棄却(D)の3態様。**面幅の粒度差**（AIは部屋一辺の合算・正解は壁セグメント単位。台所の面幅一致は4タイプとも0件）が根本
- **フィルタの正誤（部屋単位で検証済み）**: nonparty棄却=正（該当部屋の正解に非耐水の遮音壁なし）/ face_label棄却=系統誤読としては正だが
  棄却部屋の別の面に実在遮音壁があるのにAIが読めておらず結果ゼロ化 / face_cap=正だがB台所は遮@9700長大1本が残り+24.73㎡（辺長上限が必要の実証）
- 検証: 既存ゲート無影響（eval-gtype 20✅ / replay-beppu 6✅ / test 25件OK）。診断の仮定2件（LDK→台所・玄関/廊下→玄関の部屋対応）は数値整合で妥当と判断・原典部屋割りは未確認
- **次の修正候補（要設計判断・答え合わせ回避と両立させる順序）**: ①別府の収納室を壁PBから除外（正解で全タイプ部屋ブロック無し＝物件仕様）
  ②水回りの通常壁PB抑制は遮音壁耐水PB部位の実装とセットでないと数量が消えるだけ（保留案件と連動）③打放し既定の玄関・台所での過剰是正
  ④読み取り粒度（セグメント単位読み）はAI側の本丸。**①だけ先行するとDの総量が相殺崩れで-32%に見かけ悪化する**点に注意（部屋レベルでは正しくなる）

**アルファA〜F全タイプの推定パス機械テスト（2026-07-26・課金ゼロ・ロジック/読み方の切り分け完了）**:
ブラウザ同条件（専有面積入力+建物種別=新築・rooms空）を合成入力で全6タイプ再現しXLS正解と突合。**AI読みゼロ＝ズレは全てロジック起因として切り分け可能な設計のテスト**。
| type | 壁PB(枚/正) | 天井PB(枚/正) |
| A | 80/84.3 -5%✅ | 48/42.1 +14%✅ |
| B | 75/79.9 -6%✅ | 45/41.5 +9%✅ |
| C | 75/81.6 -8%✅ | 45/41.3 +9%✅ |
| D | 75/82.8 -9%✅ | 45/41.6 +8%✅ |
| E | 75/86.8 -14%✅ | 45/41.4 +9%✅ |
| F | 58/52.9 +10%✅ | 34/28.0 **+21%△** |
- **壁PBは全6タイプ✅**（-14〜+10%）＝新築式（周長×階高ベース）が全タイプで実用精度。B〜Dが同値なのは専有面積が同一(67.30)のため
- **唯一の帯外=F天井+21%はロジック起因と実測で確定**: 部屋を与えても34枚のまま不変（Aは48→46に改善）＝読み方無関係。
  真因は①新築の天井控除5.258㎡がUB/収納控除との重複回避クランプ`clamp(5.258−ub−cl,0,fill)`でほぼ消える
  ②小住戸（F=2LDK 50.74㎡・唯一の2LDK）は天井/専有比が実際は低い(80% vs A85%)のに推定式は一律＝過大が増幅。
  過大側+6枚・安全側。是正には控除設計の見直し（クランプの再検討 or 規模連動）が要るが、F単体正解への寄せ（答え合わせ）と
  紙一重のため未着手（次サイクル候補・要設計判断）
- 検証スクリプトは会話内で実行（合成入力のためリポジトリに固定していない。再現は専有・間取りをCLAUDE.md面積表から）

**建物種別の選択必須化＋リセットボタン格上げ（2026-07-26・coder/reviewer分離・承認済み・push済み）**:
実地で「57枚事故」（建物種別未選択→リノベ既定で壁PB過少）が再発した（結果画面の警告→調整→再計算80枚で回復は機能した）ため、
ユーザー決定でアップロード時の選択を必須化。Home.vueのみ+34/-17・API層不変（未指定既定'renovation'の後方互換維持）。
- 必須化: 先頭option「選択してください」(disabled)・赤「必須」バッジ・解析クリック時に未選択ならuiErrorで開始しない
  （ボタンdisabledでなくクリック時表示＝理由が伝わる設計・createProject前のガード＝孤児プロジェクトも作らない）
- リセット: 小リンク→カード群上部の.btn-secondaryボタン「↺新しい現場を開始（入力をリセット）」。hasAnyInputで表示（planDone後も押せる）・
  confirm常時化（初期状態は非表示＝無意味confirmなし）。建物種別含む全状態+sessionStorage破棄をreviewerがコードで裏取り
- Enter暗黙submit経路なし（form要素なし）・STEP2/3は建物種別チェック不要（overrides保存済み）・旧セッション''復元でクラッシュしない
- 繰越: nit hasAnyInputの空文字揺らぎ（表示のみ）/ 旧セッションplanDone+未選択の復元窓（結果画面の変更UIが受け皿・sessionStorage寿命で自然消滅）/
  途中失敗再クリックの孤児プロジェクト（既存・本差分外）
- 検証: vite build成功・バックエンド25テスト全OK（変更はfrontendのみ）
- **実地確認済み（2026-07-26）**: Aタイプとまる素材・専有71.90入力で、未選択→警告表示→調整で新築選択→再計算→**壁PB 57→80枚(-5.1%✅)**。
  building_type警告・調整UI・新築式の全経路が本番で機能。モデルはgemini-3.1-pro-preview実験を経て2.5-flashへ復帰（ユーザー操作済み・
  実測でflash>proが確定: proは展開図過大読み2.88→3.17倍に悪化）

**戸境面数の物理上限＝別府遮音PB過大残差の是正（2026-07-25・coder/reviewer分離・承認済み・push済み9022a8f）**:
面ラベル混同フィルタ後も残る遮音PB過大（D+99%/B+127%/C+37%）へ、片廊下型の物理制約で面数上限を導入。
- **導出（正解逆算でない・reviewer感度確認済み）**: 別府は片廊下型・住戸横一列（階配置表A|B|C|D|E|F|G）→
  `SOUND_FACE_CAP_PER_ROOM=2`（隣戸は左右最大2戸＝1部屋が接しうる戸境壁は2辺）・`SOUND_FACE_CAP_PER_DWELLING=6`（片側最大3室×2辺）。
  逆算ならDを±10%に収める値を選ぶはずだが**D+32.6%残存を受容**＝逆算でない直接証拠
- **削る順序**: 寄与（セグメント長/面幅）の大きい順に上限まで採用（戸境壁=奥行き方向の長い壁・大居室が主）。同寸タイは列挙順で決定的・入力順非依存をテスト固定
- **プレパス方式**: 既存2フィルタ（NON_PARTY・面ラベル混同）通過後の寄与単位を本ループと同一条件で列挙しdropセット確定。
  NON_PARTY/混同棄却面は上限枠を消費しない。棄却=「検出が無かった」扱い（警告field:'sound_wall_face_cap'で可視化）
- **結果**: D 67.32→**44.88㎡（+99%→+32.6%）** / B 60.55→53.75㎡（+127%→+101%） / **A・C・E・F不変**・**アルファ3記録DEEP IDENTICAL**
  （9記録中7がバイト不変・B/D差分も遮音3欄+警告+カウンタのみ＝削る方向のみ。reviewerが上限∞化との比較で独立裏取り）
- **正直な残差（答え合わせ回避で受容）**: B+101%はLDKの遮9.70m長大セグメント1本（面数では削れない・辺長上限=住戸奥行き突合は別サイクル）。
  C+36%は過少のAと同一面数構造＝物理的に区別不能でCだけ削るのは正解寄せになるため不変
- 検証: eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ / eval-alpha-af 567✅ / replay 10✅✗0 / test 25件全OK（新規test-sound-face-cap 33✅）。
  フォールト注入5種（上限±1・順序反転）全検出
- reviewer繰越: SF soundConsumedスキップ（デフォルト遮音ルール発火時）をプレパスが非モデル化＝上限超過住戸で実在1件余分棄却の狭い穴
  （過少側・別府正規運用pairs:[]で非発生）/ nit 上限2/6のoverride受け口なし（片廊下型でない将来物件用）/ nit 警告件数の軽微過大

**building_type未選択の警告可視化（2026-07-25・coder/reviewer分離・承認済み・未コミット）**:
壁PBが推定パス（リノベ既定）で計算されたのに未選択で気づけない問題（上記ブラウザ実測57枚の直因そのもの）の可視化。
- **実装箇所=projects.jsの/calculateルート**（実測採用の有無=sanity.okでのapplyElevationTakeoff採否はルートでしか確定しない。
  エンジン非改変＝eval/replayの警告数が構造的に不変）
- **発火条件**: building_type未指定（resolveBuildingTypeProfileと同一基準）かつ実測不採用（展開図なし/破損/サニティNG）→
  _warnings field:'building_type_default'「建物種別が未選択のためリノベーション式で推定しています。新築物件は建物種別を選択してください」。
  明示'renovation'/'new'・実測採用時・不正値（invalid警告が単独案内）は非発火。再計算で自動消滅（置換方式）
- **フロント変更なしで表示**（warnings→stores置換→MaterialResult警告パネルの既存導線・reviewerがコードで確認）
- 検証: マトリクス6条件+フォールト注入3種（✗2/✗1/✗7）・calc-wiring 58→67✅・全ゲート緑
- reviewer繰越: SF-1 実測採用時も天井PB控除には未選択が効く（天井は実測置換されないため最大約3.6枚過大で無警告・±10%内）＝
  コメント修正と情報レベル警告の要否を次サイクル検討 / nit FIELD_LABELSに新fieldのラベル未登録（生field名表示）

**残高枯渇429の専用エラー化（2026-07-25・coder/reviewer分離・承認済み・push済み）**:
2026-07-20実障害（Gemini前払い残高0の429をRPM超過と誤診し1日浪費）の恒久対策（CLAUDE.md「恒久対策②」の実装）。
- **検出**: `isCreditsDepletedMessage`＝`/credits\s+are\s+depleted|prepayment/i` + **status===429必須**（4消費箇所すべて429ゲート内）。
  RPM/PerDayの実メッセージは depleted/prepayment を含まない＝誤検出経路なし（両陰性をテスト固定・400+depleted文言も非発火）。
  Anthropicの残高切れ文言("credit balance is too low")は対象外＝非マッチで従来文言に落ちる（安全側）
- **リトライ判断を純関数化** `gemini429RetryDecision`（テスト可能に）: 残高枯渇429は**リトライせず即断念**。
  既存挙動はreviewer真理値表で**完全等価**（PerDay即断念/RPM漸増15-60秒/上限150秒/503巻き込みなし/GEMINI_RETRY_MAX既定0不変）
- **文言分岐**: aux経路=`ai_credits_depleted`（503維持・フロント互換。フロントはcodeで分岐せずmessage表示のみ＝未知コードでも壊れない）
  / 一括upload=`buildAllAiFailedReason`で「AI利用枠の残高不足・運営者にご連絡ください」（**未使用プロバイダ側のエラーでは非発火**のprovider条件）
  / タイル内訳ラベル「AI残高不足」（`classifyTileFailure`は80字切詰め前の元メッセージで判定）
- 検証: 新規test-credits-depleted 29✅・フォールト注入で8✗（全層検出）・eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ /
  eval-alpha-af 567✅ / replay3記録✅10✗0（壁92枚等の数値も既知値一致＝エンジン非影響）/ test 24件全OK / build成功
- should-fix繰越: SF-1 第2スイープがcredits_depletedタイルも再試行（残高0で最大12回の必敗呼び・課金なし・レイテンシのみ）
  / SF-2 メインupload経路のerror codeが`ai_unavailable`のまま（messageは正・aux側と非対称）

**building_typeのUI配線＝新築物件のブラウザ運用対応（2026-07-25・coder/reviewer分離・承認済み・push済み075c79a）**:
推定パスの新築/リノベ分離（materialCalculator実装済み）にUIから到達する経路が無く、新築物件でも既定'renovation'が使われ
アルファAタイプで壁PB-32.4%✗になっていた問題のUI配線。**backend-node/srcはゼロ変更**（全16ファイルmd5一致＝
WALL_PB_COEFF_RENOVATION/REDUCTION不変・後方互換はコード上自明）。
- **【正解値の誤読を是正】「壁118枚/天井61枚」は㎡の誤読**: 積算XLS原典（alpha-af-ground-truth.json・XLS100セル一致）の
  Aタイプは壁PB=118.0204**㎡**÷1.4=**84.3枚** / 天井PB=61.0072**㎡**÷1.45=**42.07枚**。README.txtの「積算118/61」表記が
  枚数に見える罠（CLAUDE.md既存記録「単位の取り違えに注意」と同型）。判定基準は84.3/42.07枚を採用
- **(1)達成の実測**: building_type='new'で壁PB 80枚(**-5.1%✅**)/天井PB 48枚(**+14.1%✅**)＝両方±15%内。
  renovation既定は壁57枚(-32.4%✗)。※天井+14.1%はrooms:[]時の新築控除非発火による既知の過大（should-fix繰越済み・±15%内）
- **UI**: Home.vue（アップロード画面・現場名/専有面積と同カード・STEP1解析時にsaveOverridesで保存・sessionStorage永続化・
  planDone後disabled）+ MaterialResult.vue（計算条件パネル・glasswool_coverageと同イディオム・「変更しない」''は既存値維持）。
  **未選択は building_type キー自体を送らない**＝既定'renovation'の後方互換（''が届いてもサーバーtrim()===''で既定扱い＝二重に安全）
- **バックエンド配線は既に通っていた**: POST /calculate→Override→overridesObj→calculateMaterials→resolveBuildingTypeProfile
  （ceiling_pb_extra_sheetsと同じ「新規パーサ不要」型）。憶測で終わらせず**疎通テスト6件**（test-calc-wiring 58→64件・
  実ルーターマウント+スタブprismaでルート経由の式切替/日本語エイリアス/不正値警告/回帰なしを固定）。期待値は式から導出
  （リノベ53枚=ceil(ceil(65.76×0.96×1.37)×0.6)・観測値の写しでない）
- **後方互換の総当たり72シナリオ**（専有30/50/70/100×1LDK/2LDK/3LDK×rooms有無×併用overrides）: override無し2回呼び・
  明示'renovation' vs 未指定、ともにバイト一致不一致0件
- 検証: eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ / eval-alpha-af 567✅ / replay 10✅✗0 / test 23件全緑（calc-wiring 64✅）/ vite build成功
- reviewer繰越nit: ①結果画面で種別変更後にHomeへ戻るとdisabledセレクトがsessionStorageの古い値を表示（表示のみ・計算は正）
  ②currentBuildingTypeの語彙正規表現がサーバーの複製（同期前提コメント有）③adjustBuildingTypeが再計算後もクリアされず同値再送（実害なし）

**別府A〜F Gemini実読みE2E（2026-07-25・課金済み・6タイプ実測・ユーザー包括承認で実施）**:
「Gタイプは問題なかったのに別府Aはゴミデータが出た」の原因究明。全6タイプを本番同一パイプライン（analyzeDrawing→attachElevationData→mergeDoorSchedule）でGemini実読みし記録保存
（`scripts/recordings/beppu-{a..f}-gemini-read-gemini-2.5-flash.json`）、別府9タイプ正解JSONと突合。**override共通**（building_type:new / glasswool_coverage:0.5 / stud_height 2720/2820 / ceiling_pb_extra_sheets:0 / closet_rc_sqm:0 / households:5 / soundWallRule:pairs[]）。
- **確定した結論＝「画像の読み取りの問題」（ユーザー指摘と一致・ロジックは正しい）**:
  **天井PBは全6タイプ✅**（A+11/B+13/C+11/D+5/E+11/F+8%）＝専有面積ベースの計算が正しい証拠。同じ計算式なのに
  **壁PB・遮音PBだけが−53〜+229%と暴れる**＝入力（AIの壁記号読み取り）が別府図面で不安定なため。
  | | 壁PB | 遮音PB | 天井PB | 展開図読取/幻覚3桁 |
  | A | +24%△ | +24%△ | +11%✅ | 9室/35個 |
  | B | −38%✗ | −21%△ | +13%✅ | 6室/8個 |
  | C | −15%△ | +84%✗ | +11%✅ | 9室/24個 |
  | D | +97%✗ | +229%✗ | +5%✅ | 8室/13個 |
  | E | −53%✗ | +76%✗ | +11%✅ | 8室/22個 |
  | F | +88%✗ | −52%✗ | +8%✅ | 0室(API失敗)/0個 |
- **誤差を3種に分解（「回数を上げれば直るか」への回答）**:
  ①**一過性API失敗**（F=展開図raw 13バイト＝空応答。A〜Eは6〜10KB正常）→ **多数決/再試行で直る**（実証: 再読みで展開図が入る）
  ②**回ごとのブレ**（遮/G枠の検出数が2〜6でバラつく→遮音PBが比例暴走。D遮3+G枠5=8箇所→+229%）→ **多数決3回読みの中央値で直る**
  ③**系統的な読み落とし**（展開図が平面13〜14室中6〜8室しか対応づかない。B45%/D43%/E43%欠落）→ **多数決では直らない**。
  毎回同じ部屋が落ちる＝**別府展開図レイアウト（面ラベルA〜D・戸境壁の描き方・部屋名表記ゆれ）に合わせたプロンプト強化が必要**（次サイクル本命）
- **私の当初仮説は外れ（scrub実験で反証）**: 「幻覚したアルファ3桁記号（C04/L14/I14/D64・A=35個混入）が遮音PBを暴発させる」と読んだが、
  幻覚3桁を機械除去（C下地=打放しのみ残す）しても**数量が1枚も変わらない**＝下流の面割付が既にこれらを捨てている＝実害ゼロ。
  遮音PBの真の暴走源は buildupCalculator.js:1499-1504（面のwall_code=L/O/W下地→遮音壁PB計上）に**別府の遮/G枠(base='W')の検出数がそのまま比例**すること
- **天井PB✅の意味**: 展開図・記号読み取りに依存しない（専有面積→天井面積→÷1.45+override）ため読み取りブレの影響を受けない。
  壁PB・遮音PBは展開図の面幅と記号に依存するため読み取り品質を直接受ける＝**部位ごとに読み取り依存度が違う**
- **次サイクル（読み取り側の改善・ロジックは変えない）**: ①多数決3回読み（①②の誤差を潰す・課金で可能）②別府展開図プロンプト強化
  （面ラベル・戸境壁・部屋名突合で③の系統欠落を潰す）。**Gタイプで実証済みのGemini単独が別府でも読めるかは、この2つを入れてから再測定**
- 検証素材: scripts/recordings/beppu-{a..f}-*.json（6タイプ実読み記録）・raw-gemini/beppu-*-*.txt（AI生レスポンス）。
  e2e-beppu-a.mjs は --type A〜F でタイプ切替（TYPE_MAP: ページ+専有面積）。専有面積は各平面図の「住戸面積」記載値
  （A83.43/B57.22/C70.12/D69.35/E67.90/F71.29。「合計面積」でなく「住戸面積」を採る）

**別府展開図プロンプト強化（③系統的読み落とし対策・2026-07-25・coder/reviewer分離・承認済み・未コミット）**:
上記E2Eで確定した誤差③「展開図が平面13〜14室中6〜8室しか読めない（毎回同じ部屋が落ちる）」への対策。**プロンプト文字列のみ**（claudeApi.js +20/-6・ロジック不変）。
- **落ちている部屋の傾向**（記録突合で確認）: 平面ではUB・洗面・トイレ・玄関・廊下・各収納まで読めているのに、展開図では上段の大居室＋洗面/トイレ6〜8室で打ち切られ、**UB(浴室)・玄関/廊下・下段の水回りが毎回落ちる**（B: UB/SCL欠落・D: UB欠落）。別府の展開図は1ページに全室段組みで詰め、下段/右端に水回り/収納の小立面を置くレイアウト＝AIが上段だけ拾って走査打ち切り＝**能力限界でなくレイアウト起因＝プロンプトで改善可**
- **追記した指示**（ELEVATION_PROMPT 3箇所+roomContextNote）: ①ページ全体走査（下段/右端の水回り・玄関・収納を段/列跨いで漏れなく・上段数室で打ち切るな）②渡した平面図部屋一覧の各室について立面を必ず探す（落としやすい室名UB/浴室/ユニットバスを明示列挙）③表記ゆれ統一（「洋室1」↔「洋室(1)」を一覧に揃える＝buildup突合を通す）④別府方式（丸囲み1文字 遮/G枠）の面ラベルA〜D説明追加。**アルファの3桁記号指示(D14/C04/I14)は1文字も削らず両対応**
- **捏造の歯止め3箇所**（過剰生成防止）: 「立面が描かれている室を飛ばすな」（存在する立面限定）/「記号読めた面だけ転記・読めなければnull・捏造禁止」/「一覧にあっても立面が無い棚スペース等は作るな」。設計原則「AIは転記のみ」に沿う
- **答え合わせなし**（reviewer確認）: 追加行の数値は記号例(C04/D14/I14/G14)のみ・別府正解値(壁PB75.5等)は不在。「読み落としを減らす一般的指示」に留める
- 検証: eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ / eval-alpha-af 567✅ / replay 10✅✗0 / test 21件全緑。**プロンプト変更で全値が1バイトも動かない**＝プロンプト以外を触っていない証拠（replay=既存記録再生・eval=エンジン計算）
- **効果測定は未実施（次サイクル・要ユーザー承認の課金E2E）**: この強化が実際にB/D/Eの欠落率を6〜8室から改善するかは実AI読みでしか測れない。**UB裏面など図面に立面が無い面はプロンプトを強化しても拾えない**（今回は「立面が描かれている室の読み落とし」対策・図面外はスコープ外）
- reviewer繰越nit-1: ELEVATION_PROMPTは平面図一覧なしの一括uploadパスでも使われるため、そちらにも「立面が無い室は作らない」の一文を対で置くと更に安全（現状も他フィールドnull許容+rooms列挙が「描かれている室」限定で実害小）

**プロンプト強化の効果測定E2E（B/D/E再読み・2026-07-25・課金約30円・ユーザー承認済み）**:
上記プロンプト強化(488da32)を別府B/D/Eで新プロンプト再読みし、before/afterを突合（before記録は`recordings/diagnostic/beppu-before-prompt/`・after は`diagnostic/beppu-{b,d,e}-after-prompt.json`に退避）。
- **結果＝効果まちまち（改善と悪化が混在・1回読みでは定まらない）**:
  | | 展開図室(前→後) | 壁PB(前→後/正) | 遮音PB(前→後/正) | 天井PB |
  | B | 6→7 | -38%→+75% | -21%→+253% | +13%✅ |
  | D | 8→8 | +97%→**±0%✅** | +229%→+338% | +8%✅ |
  | E | 8→**0(API失敗)** | -53%→+100% | +76%→-61% | +11%✅ |
- **確定した3点**:
  ①**天井PBは全タイプ✅維持**（+8〜13%）＝プロンプトを変えてもロジックの正しさは不変
  ②**プロンプト強化だけでは不十分**: Dの壁PBは±0%に大改善したが、Bは過大に反転・遮音PBは3タイプとも悪化（記号検出を促した結果、遮/G枠を**過剰検出**するようになった）。1回読みの分散が支配的で単独では「正しい資材データ」に到達しない
  ③**一過性API失敗の再現性を2例目で確認**（E=展開図raw 13バイト＝空応答。前サイクルのFと同型）＝別府展開図でGeminiが時々空応答を返す＝**多数決/再試行が必須**の決定的証拠
- **【重要な訂正】多数決3回読みは既に全E2Eで有効だった**（`wallCodeReadCount()`既定=3・2026-07-20実装）。
  私（メインループ）は「次サイクルで多数決を入れれば遮/G枠のブレが均せる」と書いたが**誤り**＝別府A〜FのE2Eは
  最初から3回読み多数決を経ている。それでも遮音PBが暴れる＝**「回ごとのブレ」ではなく「系統的な過剰検出」**
  （3回とも同じように遮/G枠を余分に拾う）。**回数を上げても遮音PBは直らない**（多数決の前提=ランダムなブレ、が成立しない）
- **遮音PB暴走の真因＝戸境壁の系統的誤検出**（D実測で確定）: DのG枠が**廊下・玄関・LDK・洋室1/3・バルコニー・防災倉庫の7室**に付く。
  だが戸境壁（隣接住戸との境界二重壁）は物理的に**住戸外周の隣戸接触面だけ**＝バルコニー・防災倉庫・室内間仕切に付くのは不可能。
  Geminiが「G枠（戸境二重壁）」を普通の間仕切壁に誤付与している＝3回読んでも同じ誤読＝多数決で消えない
- **答え合わせ回避**: プロンプトは特定正解値に寄せていない（reviewer確認済み）。効果が「まちまち」という不都合な結果もそのまま記録（Dだけ良くなったのを一般化しない）

**戸境壁の物理制約フィルタ＝遮音PB暴走の是正（2026-07-25・coder/reviewer分離・承認済み・未コミット）**:
上記で真因確定した「Geminiが戸境壁(遮/G枠)を持ち得ない部屋へ系統的誤付与」を物理制約で棄却。**多数決では消えない誤読**（3回とも同じ）に対する初のロジック対応。
- **棄却規則（`NON_PARTY_WALL_ROOM_RE`・buildupCalculator.js:117-135）**: 戸境壁を物理的に持ち得ない部屋種別を正規表現で棄却。
  屋外(バルコニ/ベランダ)・共用倉庫(防災/防炎/備蓄倉庫)・収納内部(パントリ/SCL/WCL/WIC/CL/クロゼット/押入/物入)・
  水回り内部(トイレ/便所/洗面/パウダ/UB/浴)。**触らない=居室/動線(LDK/洋室/玄関/廊下)**（戸境壁を持ちうる本物の可能性）
- **2経路に一貫適用（重要な落とし穴）**: 遮/G枠は①plan_placementsのセグメント(`soundSegments`)②face.wall_code='遮'の面記号、の2経路で計上される。
  当初①だけに入れたら**別府Dが無変化**（Dは全室D面にface.wall_code='遮'を持つ＝②経路）→②にも同じ判定を追加。片手落ち厳禁
- **効果（課金ゼロ・既存記録）**: 別府D遮音壁PB **105→89㎡(-16%・棄却3件=洗面/WCL/トイレ)** / B **66.7→60.6㎡(棄却1件=トイレ)**。
  **正解24枚に寄せていない**（物理規則で削った結果63.5枚＝残+164%は居室内の過剰検出でスコープ外・別課題）。答え合わせ回避の証拠
- **アルファ完全不変**（reviewer独立確認）: アルファ記録に遮/G枠0件・`isBeppuSoundCode`が別府マーカー付きのみ真＝構造的非発火。
  eval-gtype 20✅⏳0✗0 / replay 10✅（nonparty警告リークなし）
- **判定は normalizeRoomName（長音除去）後**の文字列に対して行う（バルコニー→バルコニ等）ため正規表現も語幹で書く。
  素の`CL`/`UB`は英字語(Club/hub)誤マッチ防止に語境界化（reviewer SF-1・全記録で発火ゼロだが将来予防）
- **残課題（次サイクル・スコープ外）**: 別府D +164%残差＝**居室（洋室3/玄関）内で遮/G枠セグメントが過剰検出**。
  物理制約(部屋種別)では削れない＝戸境面数の物理上限（住戸あたり戸境壁は隣戸接触面のみ＝2〜3面が上限）で絞る別ロジックが要る
- **多数決の限界を確定**: 遮音PBの暴走は多数決3回読み（既定・全E2Eで有効済み）で消えない系統誤読。
  回数を上げる方向は無効＝物理制約/上限ロジックが正攻法。プロンプト強化は検出を増やす方向で逆効果だった実証と整合
- 検証: eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ / eval-alpha-af 567✅ / replay 10✅✗0 / test 22件全緑（新規test-sound-nonparty 37✅）
- reviewer繰越: SF-2 2経路の棄却時の面の扱いが非対称（plan経路=通常壁PBに戻す・face経路=丸ごと落とす。設計上は一貫だがコメント補足推奨）

**面ラベル混同フィルタ＝別府D遮音PB残差の是正（2026-07-25・coder/reviewer分離・承認済み・未コミット）**:
上記物理制約フィルタ後も別府D遮音PBが+164%残った真因を特定。**別府Dだけが展開図の全8室のD面に`face.wall_code='遮'`**を持つ＝
Geminiが**面ラベル「D面」を壁記号「遮」と取り違えD列全体を戸境壁と誤認**した系統誤読（他タイプは面記号に遮/G枠なし）。
- **検出規則（`FACE_LABEL_CONFUSION_MIN_ROOMS=3`・computeElevationTakeoff内）**: 「同一face-label×同一遮音記号(遮/G枠)がN室以上」を
  面ラベル混同として該当全面を棄却。**N=3の物理的根拠**: 正当な2室共有（隣接2室が同一戸境壁を両室の同face-labelで持つ）は保持し、
  3室以上が同一face-labelで戸境壁を持つのは住戸3辺以上が別隣戸に接し作図者が全て同ラベルを振る必要があり物理・作図上ほぼ起きない。**正解24枚からの逆算でない**（Dは8室一律でN=3でも8でも棄却）
- **面記号経路のみ対象**（plan_placements経由の遮/G枠=別府A/B/Cのセグメント計上は正当＝面ラベルを持たず対象外）。
  前サイクルのNON_PARTY棄却とはカウント時に水回り/収納を除外して二重カウント回避
- **効果（課金ゼロ）**: 別府D遮音壁PB **82→64㎡(-13枚・face_label_dropped=5=玄関/LDK/洋室1/2/3)** / **A/B/C/E/F完全不変**（reviewer構造的保証）。
  正解24枚に寄せず（after 45枚=+89%残）＝答え合わせ回避。残+89%は面ラベル混同で本物の戸境壁も落ちた読み取り側の限界（黙って消さず`_warnings`で手動追加を促す）
- **アルファ完全不変**（reviewer独立確認・3桁記号でbeppuマーカー無く混同集合が常に空）: eval-gtype 20✅⏳0✗0 / フォールト注入（閾値3→2で5アサーション赤化＝検出力実証）
- 検証: eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ / eval-alpha-af 567✅ / replay 10✅✗0 / test 23件全緑（新規test-sound-face-label 22✅）
- reviewer繰越: SF-1 G枠も棄却対象（正当なG枠が3室同face-labelに出る物件が将来あれば誤棄却・現状は遮とG枠を分離キーで到達しにくい）/ SF-2 戸境3辺以上の変形住戸を稀に過剰棄却（face-label分散度併用の余地）

**別府主要3部位の現到達度（2026-07-26訂正・面数上限適用後・遮音は㎡単位に是正）**:
**【訂正】旧表の遮音PB列は単位混同の誤値だった**（資材行「遮音壁PB張り」は**㎡**なのに枚の正解と比較。「A遮音+3%✅」は誤り＝
実際は-26%過少。壁/天井の枚単位比較は正しい）。CLAUDE.md既存の「単位の取り違えに注意」の罠の資材行版に再びかかった。
| タイプ | 壁PB(枚) | 遮音PB(㎡・訂正) | 天井PB(枚) | 記録 |
| A | +31%✗ | -26%△ | +11%✅ | 元(プロンプト強化前) |
| B | +75%✗ | +101%✗(面数上限後) | +13%✅ | 再読み(強化後) |
| C | -15%△ | +36%✗ | +11%✅ | 元 |
| D | **+0%✅** | +33%✗(面数上限後) | +8%✅ | 再読み(強化後) |
| E | +100%✗ | -100%✗(展開図欠落) | +11%✅ | 再読み(強化後・API失敗) |
| F | +88%✗ | -100%✗(展開図欠落) | +8%✅ | 元(API失敗) |
- **表の遮音㎡は全て現HEADのエンジン実行値**（computeElevationTakeoff・overrides=stud2720/2820,pairs:[]。旧%の÷1.4再基準化ではない。
  Aの-26%が旧+3%÷1.4と一致するのは偶然）。**A遮音の時点差に注意**: 下記A節の「-14.8%（34.5025㎡）」は**NON_PARTYフィルタ導入前**の値
  （手計算34.5025=フィルタ前と一致で証明済み）。現HEADは洗面室G枠がnonparty棄却され29.92㎡=-26.1%。C+36%も現HEAD実行値（35.36㎡・記録から再現可）
- **主要3部位同時±15%は0タイプ**（Dが壁✅天井✅の2/3で最接近。訂正によりA遮音は✅→△に降格＝旧表の「A 2/3」は単位混同による過大評価だった）
- **天井PBは全タイプ✅**（+8〜13%）＝ロジックの正しさ不変。壁PBはDが±0%✅
- **機械再現: `scripts/replay-beppu.mjs` 新設済み（2026-07-26・coder/reviewer分離・承認済み）**: 別府6記録を本番同順で再生し
  現HEADスナップショット（枚±0/㎡±0.02/カウンタ±0）と突合、ズレたらexit 1。**正解乖離%は情報表示のみ**（答え合わせ判定なし）。
  単位を出力に明記（壁/天井=枚・遮音=㎡）＝単位混同の再発を構造的に防止。フォールト注入（面数上限∞化→B/D✗+カウンタ検知）で検出力実証。
  **エンジン変更後は必ず実行**し、別府数値が動く変更は差分明記のうえスナップショット更新。
  このスクリプトで判明した手転記時代のズレ2件: ①B天井は+13%でなく**+15.1%**（表を正とせずreplay出力を正とする）
  ②**E/Fの遮音は資材行では0でなく13㎡[推定]**（展開図欠落→推定パスのアルファ実績固定値 materialCalculator.js:2408 が出る。
  表の「-100%(展開図欠落)」は実測ゼロの意味・資材行としては-71.8%/-65.2%）
- reviewer繰越（replay-beppu）: SF-1 soundWallRule:{pairs:[]}は本番未配線のE2Eプロファイル専用（現記録では未指定と数値完全一致を実証済み・
  将来幅1450±80の面が出ると本番とreplayが割れる→スクリプトに注記済み）/ N-1 sanity分母のAREASフォールバックは本番に無い（不活性・注記済み）/
  N-2 表示の警告数はエンジン警告のみ（ルート層warning含まず）
- **けいとさん確認事項は当面保留（2026-07-26ユーザー決定）**: 巾木H=40vs60表記・別府遮音壁耐水PB35㎡・材積4行の表示・遮音シート行の要否。
  以後この件でユーザーへ催促しない（必要になったら向こうから動く）
- **プロンプト強化(488da32)は効果まちまち＝net負の疑い**（Dの壁PB改善+97%→±0%と引き換えにB遮音を悪化）。revert可否は要ユーザー判断
- **壁PB/遮音PBの残差は展開図の読み取り品質（欠落率・記号過剰・長大セグメント）が支配**＝AI読み取り側。ロジックで削れる系統誤読
  （戸境壁2種+面数上限）は削り切った（D遮音+99%→+33%）

**別府AのGemini実読みE2E＋遮/G枠のセグメント計上（2026-07-25・coder/reviewer分離・承認済み・push済み・実費約10円）**:
別府Aタイプの3図面をGeminiで実読み（`e2e-beppu-a.mjs`新規・記録`recordings/beppu-a-gemini-read-gemini-2.5-flash.json`）。
**別府の壁記号（丸囲み1文字 遮/G枠/A/C04）はGeminiが寸法mm付きで正しく読めている**（読み取り14部屋・展開図9室・建具20件・
タイル失敗0）＝プロンプトは別府方式に対応済み（条件(3)合格）。ズレの原因はロジックと確定（条件(2)）。
- **根本原因（私の当初診断を訂正）**: 記号はplan_placementsに結合済みだった。真因は**別府展開図が面幅を合算**（LDK-A面=8200mm）する一方、
  タイル記号は**per-segment長**（遮@3250）で読まれ、既存の「placement寸法≒面幅±80mm」マッチが合算面で成立しなかったこと
- **修正**: 遮/G枠（`isBeppuSoundCode`=beppuマーカー付きW系）を面幅マッチから除外し**セグメント長で直接計上**
  （sound_wall_pb += len×下地高・GW・遮音シートも）。セグメント幅分を既定G14へ落ちる面の壁PBから差し引く（soundDeductByFace）。
  周長・巾木・開口はfull width維持（戸境壁も物理的にその部屋の壁のため）。両面計上は各室placementが両側を持つことで自然成立
- **別府A before→after（※当時値＝NON_PARTY/面ラベル/面数上限フィルタ導入前。現HEADはA遮音29.92㎡=-26%・現到達度表参照）**:
  遮音壁PB -76%→**+26%**（符号反転・当時の集計基準による表記で現表の-26%とは時点も基底も別物）/ GW -86%→**-29%** / 壁PB +98%→+82%（方向改善）/ 天井PB+11%✅。
  **壁PB+82%残差は面のper-wall合算 vs placementのper-segmentのgranularity差**（面レベル記号無しでは安全に解決不能・次サイクル候補）
- **答え合わせでない（reviewer手計算で確認）**: 遮2面×3.25×2.57+G枠(LDK/洋室3=2.57・洗面室=2.77)×2.25=34.5025㎡=エンジン出力。
  正解40.5に対し-14.8%（正解を上回らない=逆算でない直接証拠）。係数調整ゼロ・placementのwall_length_mmをそのまま拾うだけ
- **C04差し引き却下が正しい（reviewer確認）**: 打放しC04差し引きも試作したがアルファのノイズC04で92枚→54枚(-38%)退行→撤回。別府の数字を追わずアルファを守った
- **アルファG完全不変（最重要・reviewerがバイト一致証明）**: 遮/G枠placementはアルファ記録に0件＝新分岐非発火。
  eval-gtype 20✅⏳0✗0 / replay3記録とも壁92枚不変 / computeElevationTakeoff全出力DEEP IDENTICAL
- should-fix繰越: SF-1 同一戸境壁が3室以上でマッチすると両面超の計上リスク（現状は過少側で実害なし）/ SF-2 partition_face_length参考値がdeduct面でwPb使用
- 検証: eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ / eval-alpha-af 567✅ / replay3記録✅10✗0 / test-buildup-placement 69✅（別府5件追加）/ 全20ユニット緑
- **壁PB残差を+82%→+24%に（別府打放しC04の限定除外・同2026-07-25・push済み）**:
  真因は「打放し記号(C04/D/E)が読めているのに面幅マッチ不成立で記号未割付→既定G14＝全面PB」。36面中28面が既定G14で237㎡計上。
  前サイクルで「C04差し引き」を全記録一律でやったらアルファのノイズC04で92枚→54枚(-38%)退行→撤回。
  **今回の安全版**: `isBeppuLayout`（図面に`遮`/`G枠`が読めているか＝アルファに物理的に存在しない記号）が真のときだけ、
  「打放し記号を読めた部屋の記号未割付面」を既定G14→打放し(base:'C',mid:0・PBなし)へ倒す。二重の条件（遮/G枠マーカー＋
  部屋にC下地が読めた）でアルファ非発火。**答え合わせ回避**: 全default面打放しなら61.8㎡(正解75.5を下回る過剰差し引き)だが、
  読取根拠のある部屋だけに限定して93.09㎡=+24%(過大側)を受け入れ。LDK等C04未読の部屋は全面PB維持。
  reviewerがHEADとバイト比較し**差分はbeppu wall_pb 136.39→93.09の1行のみ・アルファ3記録完全一致**。test-beppu-blank-default 6✅
- nit繰越: 打放し既定`surf:0`で倒れた面の壁クロス(cloth_sqm)が計上漏れ（別府Cは実際クロス貼surf:4）。ただし壁クロスは
  filterKenzaiScope外で画面非表示+beppu正解10部位に無くeval不変＝実害なし。将来クロスを表示スコープに入れるなら記号ごとにsurf分岐要
- **未実施（次サイクル）**: 遮音壁耐水PB35㎡（別府独立部位・zairyo_itemマッピング無し・けいとさん確認）/ B〜Fの実読み（壁PB残差が他タイプでも同じか・残る+24%はLDK等でGeminiが打放しを読めていない読取側の限界）/ 実AI読み取りで残る戸境壁を拾えるか

**EV廻り壁PBの丸めをceil→roundに（2026-07-25・coder/reviewer分離・承認済み・push済み）**:
面積換算部位の丸め横断調査で、per-戸ceilが総量方式（XLS AB列=W/X・全戸合算÷係数）から**±8%以上乖離するのは
EV廻り壁PBのみ（+38%）**と確定（耐水/遮音+7%・壁PB/天井PB+1%は対象外）。EV実測2.16㎡→ceil(1.44)=2枚→**round=1枚**。
- **推定パスは丸め箇所が無かった**（`evWallPb95Sheets=3`固定＝実測0時フォールバック）。丸め対象は実測パス2箇所（行+summary）のみ。
  私（メインループ）の「推定パスも直せ」指示をcoderが訂正（÷1.5のceilが実在しないことをgrep確認）
- **±5%は構造的に達成不可能**（1.44枚/戸の極小量でper-戸round×9=9枚 vs XLS総量ceil13枚=-30.8%。ceilの+38.5%よりは近い）。
  ユーザー確定でround採用。答え合わせ回避（13枚に合わせず残差-30.8%を明示）
- **should-fix即修正（round化が持ち込んだ潜在穴）**: `0<ev<0.75㎡`帯でround=0になり「実測が固定推定3枚に化ける退行＋行/summary不整合」。
  `evPbSqm>0 ? Math.max(1,round(...)) : 0` で最低1枚保証＋行とsummaryを同一変数に統一。既知正解は全て0.75㎡超で現データ非発火だが塞いだ
- 検証: eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ / eval-alpha-af 567✅ / replay3記録✅10✗0（Claude記録2→1枚[実測]）/
  test-ev-wall-pb 19✅ / reviewer独立46,656ケースでEV以外バイト単位不変（壁PB・天井PB・収納面PB・材積4行すべて不変）
- **丸め横断整合はこれで完了**（実害レベル+38%の部位=収納面PB・EVを両方解消）。残る耐水/遮音+7%は閾値内で対象外
- **戸数総量方式をopt-in追加（条件±5%を文面どおり満たすため）**: per-戸round×9=9枚 vs XLS総量13枚=-30.8%は
  極小量では埋まらない（エンジンは戸数を持たない=1戸単位見積が根幹）。`overrides.households`で戸数を受け取り、
  **指定時のみ総量方式** `ceil(evPbSqm×households÷1.5)`（XLS AB列=W/X厳密一致・戸数9で13枚=0%）。
  **未指定/0/負/非数/小数はper-戸roundにフォールバック**（1戸単位見積の根幹を壊さない・1216ケースでバイト単位不変）。
  答え合わせでない（13枚に合わせず総量方式の式そのもの）。projects.jsから配線・入力UIは未追加（opt-inのバックエンド配線のみ）

**収納面PBを固定5枚→面積換算に（2026-07-25・coder/reviewer分離・差し戻し1往復・承認済み・push済み）**:
収納面PB（マルチクロゼット・WIC・CLRC面 石膏ボード）が固定5枚（ALPHA_STATS 340/67）で面積連動しなかった問題。
拾い面積は既に持っていた（closet_rc_wall=67.59㎡/9戸=7.51㎡＝集計表C73と一致）ので**÷X73(1.45)の面積換算**へ。
- **差し戻し1回（丸めの罠）**: 第1版は`ceil(7.51/1.45)=6枚`で**+15.8%**＝旧固定5枚(-3.5%)より悪化。
  eval-gtype-buildupは「クロゼット内RC面(㎡)」を判定していて+1.3%✅だが、**面積の判定であって発注枚数の判定ではない**。
  実際に発注される枚数が+15.8%ずれていた（緑≠正しいの典型・evalの見る対象に注意）
- **是正=round採用（総量方式の裏取り）**: XLS実発注は総量方式 `ceil(7.51×9戸÷1.45)=47枚`。per-戸ceil(6)×9=54枚は+15%過大、
  per-戸round(5)×9=45枚が真値47に近い。CLAUDE.md 2026-07-17丸めメモ「戸単位ceilは過大・総量方式へ」と整合。
  round(5.18)=5枚→**-3.5%✅**。答え合わせでない（逆算なら7.51÷5=1.502を採るが原典1.45経由でround=5・残差-3.5%を受容）
- **物件依存**: 別府は収納面PBの概念が無い（作り付けシステム収納・集計表r73は木胴縁で別用途）。`overrides.closet_rc_sqm='0'`で0枚。
  `resolveClosetRcSqm`は界壁面`resolveKaibeWallSqm`と同一流儀（正規表現・0許可・符号反転/10倍化を起こさない入力ガード）
- **丸めの非対称（should-fix繰越）**: 面積換算する大数量部位（壁PB/耐水/天井PB）はceil、小数量の収納面PBはround。
  **EV廻り壁PBも小数量で同じper-戸ceil乖離(+38%)を持つがceilのまま**＝真の反例（reviewer指摘SF-1）。EVは実測ベース+「隣接住戸のみ計上」で別性質のため今回スコープ外
- SF-2是正: buildupのコメントに`opts.closetRcSqm`という**実装のない受け口**が書かれていた（実測パスはkonpane_sqm使用）→削除し実態に合わせた
- should-fix繰越: SF-3 実測パステストがkonpane_sqm=9.5でround/ceil区別せず（7.51や端数<0.5で担保可）/ SF-4 別府で0枚の行が画面表示（軽微）
- 検証: eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ / eval-alpha-af 567✅ / replay3記録✅10✗0 / test-closet-pb 24✅ /
  reviewer独立33,000+5,000ケースで収納面PB以外バイト単位不変 / 別府0枚 / build成功

**材積係数のXLS整合サイクル1＝eval期待値の是正（2026-07-25・coder/reviewer分離・承認済み・push済み）**:
「材積係数が診断+3.4%/+4.4%（過大）なのにevalで-12.6%/-15.1%（過少）」という逆符号の食い違いを調査(0)した結果、
**主因は係数でなくevalの期待値が誤基準**だった。eval-gtype-buildup.mjs の材積期待値が**見積明細÷67戸（A〜H全タイプ平均）**を
使っており、Gは67戸中で小さめのタイプなので平均が+18〜20%上振れしていた（KP+47%と同型の期待値監査漏れ）。
- **是正**: 材積期待値を見積明細平均→**XLS集計表②G.XLSのG単体（Y=W×X）**へ差し替え。間仕切木軸 1.1537→0.975351（C52=84.082×X52=0.0116）
  / 天井下地 0.5746→0.478608（C77=59.0874×X78=0.0081）/ 木胴縁 0.0537→0.049461（C85=5.047×X86=0.0098）。
  ※Y式のX参照は同行でなく次行（X78/X86）である点まで原典で確認。際根太0.0246は元からG単体（是正不要・丸めのみ）
- **逆算でない証拠**: エンジン出力（1.008/0.4881/0.9299）と期待値（原典C×X）が**完全一致していない**（残差+3.3%/+2.0%/-4.7%）。
  答え合わせならexpected=engineにするところ、XLS由来値を採り残差を残している。reviewerがエンジン生出力と「逆算なら採る値」の一致を実測して裏取り
- **tolは緩めず厳格化**（材積 35/45→10）。67戸平均+広tolが覆い隠していた誤差を可視化。フォールト注入（MAJIKIRI係数+20%）でeval exit 1＝生きたガード
- **横断チェック**: 全19期待値の出所を1つずつ照合。誤基準は材積4項目のみ、面積/長さ系11項目は既にG単体C列一致
- 検証: eval-gtype 20✅⏳0✗0（材積4項目 際根太+4.6%/間仕切木軸+3.3%/木胴縁+0.1%/天井下地+2.0%）/ eval-beppu 453✅ /
  eval-alpha-af 567✅ / replay3記録✅10✗0 / test-*.mjs 全17件緑。変更は eval-gtype-buildup.mjs のみ（src・test不変）

**材積係数のXLS整合サイクル2＝係数を材長モデル→係数直接方式へ（2026-07-25・coder/reviewer分離・承認済み・未コミット）**:
サイクル1で残した「check-engine-constantsの間仕切下地0.012/XLS0.0116・天井下地0.00846/0.0081」を解消。
- **案B採用（材長×断面 → 拾い面積×材積係数m³/㎡直接）**: XLS原典は `Y=W(拾い面積)×X(材積係数)` で材積を直接算出＝
  エンジンの「材長(幾何モデル8.89m/㎡)×断面0.00135」は原典に無い別式だった。`MAJIKIRI_M3_PER_SQM=0.0116`(X52)・
  `CEILING_M3_PER_SQM=0.0081`(X78)を新設し木胴縁`DOBUCHI_M3_PER_SQM=0.0098`と同型に。**案A（幾何モデル整合）は間仕切で相殺を崩し悪化するため不採用**
- **天井のY77→X78クロス参照**（自行X77=1.45でなく1行下のX78=0.0081を参照する非自明な数式）をreviewerが原典で裏取り＝逆算では出ない原典理解
- **相殺は崩れていない**: 適用後-7.8%は「材積%誤差=拾い%誤差(-7.84%)」の恒等的必然（推定パスと適用後が同じ-7.8%）。
  木胴縁型の+78%反転は起きない。test-timber-volume-coeff（22✅・dobuchi型のセット修正回帰ガード）で「材長モデルへ逆戻り/片方だけ直すと悪化」を固定
- **材長モデルは残置**（majikiriTimberLengthM等・calculation欄の`≒747m`表示専用に実参照あり＝死にコードでない。数量には無影響）
- **条件達成**: check-engine「2物件一致だがXLSとズレ」→**なし**（間仕切下地・天井下地とも+0.0%）。
  eval材積 間仕切木軸-0.1%/天井-2.3%（サイクル1の期待値是正と合わせ全±10%内）。reviewer独立1536シナリオで材積2行以外バイト単位不変
- **拾い面積-7.8%はスコープ外**（展開図読み取り層の構造差＝洗面UB囲い壁等が展開図外。係数でなく拾い層の別問題）
- SF-1是正: check-engine-constantsのengineNameラベルが旧`×断面`表記で陳腐化していた→`MAJIKIRI_M3_PER_SQM(XLS X52直接)`へ更新
- 検証: eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ / eval-alpha-af 567✅ / replay3記録✅10✗0 / test-*.mjs 全18件緑

**別府「ｽﾗﾌﾞ下り際根太 H=210」の欠落を追加部位行で解消（2026-07-25・coder/reviewer分離・承認済み・未コミット）**:
別府にしか存在しない部位行（集計表r10・別府7タイプに23.3〜25.7m/戸・H/Iは0・アルファには行自体なし）が
資材リストから丸ごと欠落していた問題。**定数overrideでは埋まらない**（際根太本体H110とは別材・X10=0で材積換算せず）ため
`resolveExtraParts`（`overrides.extra_part_N_*`・N=1〜10）で**任意の追加部位行を外から与える器**を新設。
- **転記層に徹する設計**: 部位名/数量/単位/規格/カテゴリ/材積換算の有無を全て外から指定。係数・推定モデルを持たない
  ＝別府7タイプ全て+0.00%（入力値=出力値）で答え合わせ不能。`kiwaneta_spec`流用は禁止条項どおり不採用（H=210は別材）
- **0で行を出さない**（H/Iは行なし）・**未指定なら1行も増えない**（旧実装HEADと750ケースbyte-identical・reviewer独立確認）。
  qty=0→skip / name空→行なし / 材積は`_volume='あり'`+係数指定時のみ・0除算/0m³行は抑止
- **入力ガード**（過去の天井PB override事故の教訓）: 符号反転`-5`・小数10倍`1e3`・`23.3m`は全拒否（正規表現+範囲検証）。
  単価は`unit_price:0`明示で際根太350円/mの部分一致継承を回避
- **check-engine-constantsを実行アサーション化**（手書き✅でなくcalculateMaterialsを走らせて判定）＝M-2型「緑の嘘」を構造的に防止。
  「片方の物件にしか存在しない部位行」セクションの❌が0件に
- **should-fix（要けいとさん確認）**: 表示スコープの非対称＝スラブ下りH=210(extra_part)は画面/Excelに出るが
  際根太本体H110は従来どおり非表示。同じ木下地ブロックの兄弟行で可視性が食い違う（数量は正・表示範囲の話）
- 検証: eval-gtype 20✅⏳0✗0 / eval-beppu 453✅ / eval-alpha-af 567✅ / replay3記録✅10✗0 /
  test-extra-parts 28✅・test-calc-wiring 58✅・既存ユニット全緑 / 別府7タイプ±2%（実測+0.00%）

**別府4丁目で定数の物件横断検証（2026-07-24・coder/reviewer分離・差し戻し1往復・承認済み・未コミット）**:
「どんな図面でも正しい資材リストを」の設計見直しの一環。別府9タイプ正解（beppu-9types-ground-truth.json・90セルXLS一致）を使い、
**エンジンのハードコード定数がアルファ以外で通用するか**を機械検証。**実AIゼロ**（evalはエンジンを一切importせず・改竄注入で✗検出を確認＝答え合わせ不能）。
- 新規: `scripts/eval-beppu-truth.mjs`（正解JSONの構造+換算係数を453項目検証・エンジン非依存）、
  `scripts/check-engine-constants.mjs`（定数のアルファ/別府横断チェック表・md5でXLS陳腐化検知）
- **換算係数1.4/1.45/1.5は2物件一致**（壁PB/耐水/遮音÷1.4・天井系÷1.45・界壁系÷1.5）。ただし**両XLSは同一テンプレートの使い回し
  （シート構成・行番号レイアウト完全同一）＝「業界標準・物件不変」ではなく「テンプレート既定値」**。X9/X73/X81が別府で上書き済み＝
  テンプレート値は物件ごとに書き換わる実証あり。ラベルは「物件不変」でなく**「2物件一致（n=2）」**に留める。
- **物件依存で未対応2件**（他物件で誤る）: ①際根太規格（アルファ45×30 / 別府H110・別府には「スラブ下り際根太H=210」という概念自体無い行も存在）
  ②天井PBﾊﾟｳﾀﾞｰ･ﾄｲﾚ加算+4枚（別府は集計表74行が空＝加算行が無いのに`POWDER_TOILET_PB_SHEETS=4`が部屋名の存在だけで無条件発火＝別府では常に+4過大）
- **対応済み物件依存3件**: 下地高（一般2.57/水回り2.77）+遮音壁ルール（opts.studHeight / opts.soundWallRule でoverride可）
- **レビューで是正した私（メインループ）の誤報告3件**（一致＝正しいの罠。KP+47%事故と同型）:
  ①「巾木H40 vs 60が物件依存」は誤り＝**両物件ともH=60主体**（アルファでH=40は玄関4.8m/56.44m=8.5%のみ）。むしろ集計表(H=60)と
  見積明細/CLAUDE.md(「木製巾木 H=40」3,615m)の**表記不整合**が別論点（要けいとさん確認）
  ②「界壁1.5/収納面1.45が物件不変で+0%安全」は捏造＝**エンジンにその係数は存在しない**（一部界壁=固定3枚・収納面=340/67実績）。
  実測置換されるEV廻り壁PBは buildupCalculator.js:1236 が÷1.4だがXLS X62=1.5＝**-6.7%ズレ（既知A-4そのもの・「安全」は逆）**
  ③「木胴縁-69.7%は係数を直せば⏳-46%解消」は誤り＝拾い面積過大との**相殺で-46%に見えている。係数だけ直すと+84.5%に反転**（A-2下地高と同型・セット修正必須）
- **実害として確定（次サイクルsrc修正候補）**: ①天井PBクランプ[20,50]は**実測モードでも常時効く**（applyElevationTakeoffに天井へのset()が0件）＝
  別府H=65.3/I=70.9枚が50枚頭打ち(-23〜30%)。②EV廻り÷1.4→X62=1.5化(A-4)。③木胴縁の係数+拾い面積セット修正。
  ④従来パス（展開図なし）の実績クランプ5件が別府全滅級（間仕切GW[5,15]に別府35〜54で9/9帯外・巾木[30,60]に別府44〜107で7/9帯外）
  ＝アルファのリノベ実績帯が別府の新築規模に合わない。⑤buildupCalculator.js:66「業界標準で物件不変」コメントの過信表現
- **依然未検証**: 別府の**図面読み取り精度**（parsedData無し＝実AI課金が必要）

**別府のアップロード素材を整備＋壁記号の方式差が判明（2026-07-24・課金ゼロ）**:
`別府_タイプ別アップロード用/`（9タイプ分の空フォルダ）に3図面を配置完了。PDFの**テキスト層**から図面名を機械抽出して特定（AI不使用）。
- **ページ対応**: 平面詳細図 A=36 B=38 C=40 D=42 E=44 F=46 G=48 H=50 I=54 ／ 展開図 A=37…G=49・**H=51+52 / I=55+56（2枚）** ／
  木製建具表=**p69（全タイプ共通）**。※アルファと違い建具表は1枚に集約
- **【重要】PNG化はPyMuPDF必須**（`scratchpad/conv_mupdf.py`・dpi=400で6617x4678px）。当初 pdfjs-dist+node-canvas で131p変換したが
  **線と枠は出るのに文字（部屋名・寸法・凡例）が全欠落**した画像になっていた（エラーなし・サイズも妥当なので気づきにくい）。
  原因はJWW由来の**Unicode私用領域(U+E000〜)カスタムフォント**でnode-canvasが解決できないため。registerFontでも代替不可。
  **これに気づかずアップロードしていたら「AIが読めない＝読み取り精度が低い」と誤診していた**。変換後は必ず目視確認すること
- **壁仕上の表記方式がアルファと根本的に違う（実装課題）**: 別府に**3桁記号（G14/C04等）は存在しない**（p48のテキスト層で検出0件）。
  凡例（p48実測）= `遮`戸境二重遮音壁(胴縁+PB9.5+GW24K+遮音シート+PB9.5) / `Ｇ`戸境二重壁(胴縁+PB9.5+GW24K) /
  床は丸数字①磁器質タイル②複合フローリング③ビニル床タイル+耐水合板 / 壁仕上はアルファベット1文字で
  「ビニールクロス貼PBt9.5」「同（GL工法）」「同・コンクリート打放し補修（＝PBなし）」等を引く方式。
  PB計算に必要な区別（PB有/GL工法/PB無/遮音）は**別府にも全部あるが表記が違う**ため、
  `analyzeWallCodesTiled`/`normalizeWallCode`（アルファの3桁専用）は別府で1件も読めず**全面PB扱いになる**。
  → 壁記号読み取り層に別府方式のマッピングが必要（次サイクル本命・課金ゼロで実装可）

**別府記号レビュー指摘の修正サイクル（2026-07-24・coder/reviewer分離・課金ゼロ・未コミット）**:
別府1文字記号マッピングの条件付き承認（must-fix1/should-fix4）への対応。**新設した警告はすべてアルファで非発火**
（⑤のみアルファでも発火＝既定ルールが実際に正しく効いているため意図どおり）。
- **①遮音シートのデッドエンド → (b)警告で可視化を選択**（資材行は新設しない）。理由: 発注用の独立行が
  **拾い出しXLS・見積明細のどちらにも存在しない**＝名称/摘要/単価の正が無い（別府 集計表54行「遮音壁ＰＢ張り」の
  規格は"t9.5+GW"でシート抜き。シートは30行「EV面遮音壁（界壁）PBt9.5+9.5GW+ｼｰﾄ+木胴縁」・34行の**複合行の
  規格文字列の中にしか出ず単独の数量・単価を持たない**）。`_warnings` field:'sound_sheet' で拾った㎡を明示し手動追加を促す
- **④Ｇ既定（打放し）が外れても気づけない → `beppu_g_shape_unknown` カウンタ+警告** field:'wall_code_g_shape'。
  面/plan_codes/plan_placementsの3経路で素の'G'を計数（四角囲みは canonicalizeWallCode が 'G枠' へ畳み込み済み＝
  形状が判別できた分は数えない）。図面実測ではＧは四角囲み（丸囲みは遮のみ）＝**既定が外れる公算が高い**ため、
  静かな過少（サニティは上限しか見ない）を可視化する
- **③Ｂ(GL工法)→ev_wall_pb の桁乖離を警告** field:'ev_wall_pb'・閾値5㎡（実績上限2.2㎡の約2倍）。
  XLS実績は**戸当0.8〜2.2㎡**（別府 集計表62行「防露ふかし壁（ＰＢ）」A=1.36/B=1.904/C=0.816/D=1.904/G=2.176）なのに
  エンジンはＢが付いた壁を1面まるごと計上するため1面10.03㎡・2面16.38㎡（7〜12倍）になりうる。
  **粒度差（Ｂが指すのはRC面の一部で壁1面全部ではない）は未解決**＝方向は正しいが桁は合っていないことを隠さず出す。
  アルファ実測2.16㎡では非発火
- **⑤既定遮音ルール（アルファGタイプ専用のLDK↔洋1 1450/LDK↔洋3 1050）の発火を警告** field:'sound_wall_rule_default'。
  別府にも同名の部屋が実在し幅が帯に入れば誤発火するため。opts.soundWallRuleを明示指定した場合（別物件ルール・pairs:[]）は非表示
- **②集約層の門番厳格化を記録**（未報告の挙動変化だった）: 旧 `/^[A-Z][0-9][0-9]$/` は下地英字を見ないため
  'T14'/'X99'等の凡例外3桁を通していたが、新門番 parseWallCode は VALID_BASE_CODES='ZCDGHILSOW' で弾く。
  下流影響ゼロ（旧経路でも面割付時にnullで無視）＝改善だが「アルファ完全不変」の報告としては不正確だった。
  コメント明記＋固定テスト（C12/C13）を追加
- 検証: **アルファ完全不変**（eval-gtype-buildup 19✅⏳1✗0 / replay3記録✅10✗0・壁PB92枚・天井42/41/41・遮音12.85㎡が
  修正前後で完全一致）・eval-beppu-truth 453✅ / test-beppu-wallcode 101→**125✅** / 既存ユニット全緑（ceiling-pb-clamp75・
  calc-wiring39・buildup-placement61・opening-match64・stud-height93・takeoff-sanity51・tile-pool13・tile-failures34・
  wallcode-vote53・wallcode-reread15）。実AI呼び出しゼロ

**天井PBクランプ→比率サニティ化（2026-07-24・coder/reviewer分離・差し戻し1往復・承認済み・未コミット）**:
別府での実害バグ修正サイクル1件目。天井PBクランプ `Math.min(Math.max(x,20),50)`（アルファのリノベ実績帯[20,50]）が
**実測モードでも常時効き、別府H=65.3/I=70.9枚を50枚で頭打ち（-23〜30%）**にしていた（applyElevationTakeoffに天井へのset()が0件＝
天井PBは実測置換されず推定値がクランプされる）。
- **是正の経緯（緑≠正しいの実例）**: coder第1版はクランプ撤去し「過大側はvalidateTakeoffSanityが担保」とコメント→**虚偽根拠で差し戻し**。
  reviewerがコードで実証: ①applyElevationTakeoffは天井を上書きしない ②validateTakeoffSanityは壁PB比率しか見ず天井PB非対象・
  かつprojects.js:965の展開図ブロック内でしか呼ばれず**従来パスでは未呼び出し** ③**従来パス+専有未入力+外形寸法誤読200㎡→天井138枚に暴走を再現**
  （既知バグ「外形寸法誤読による専有面積の上書き」と同経路・旧クランプが唯一の上限ガードだった）
- **採用した実装（materialCalculator.js:825-908付近）**: 専有面積との**比率型サニティ3段**。分母sanityBase=
  検証済みsource(user_input/ai_label_roomsum_verified/ai_estimate_verified)→declaredArea、未検証はdeclared≤150㎡(PLAUSIBLE_MAX_FLOOR_SQM)なら信頼・
  超なら誤読とみなしroomsSumAreaへ切替（正常な拾い落ち補填=total65.76と誤読total200を分ける鍵。roomsSumを常用すると誤発火）。
  ①ceilingArea>sanityBase×1.3→天井面積をsanityBase×0.88(収束比・別府/アルファ共0.88)へ丸めて再計算+警告ceiling_pb_area_inflated
  ②分母全滅時は絶対上限100枚(=天井145㎡・別府I=71枚を弾かない)+警告ceiling_pb_absolute_cap ③過少<×0.4は情報警告のみ(S-1)。
  calcWarnings→_warnings→projects.js:1024で従来パスでも永続化（前回の穴を塞いだ）
- **reviewerの実証（before/after）**: 誤読200㎡→131枚(暴走)が**40枚**に、分母汚染時138→**100枚**に抑制。別府H=66/I=71枚は無警告通過・
  Gタイプ42枚不変。ガード無効化でtest 7件✗＝空振りでない。**eval 19✅⏳1✗0 / eval-beppu 453✅ / replay3記録✅10✗0 /
  test-ceiling-pb-clamp 29✅ / ユニット全緑**
- **should-fix繰越（非ブロッキング・通常運用=専有入力なら非発生）**: SF-1 中間帯の誤読(total130㎡・真値65)が無警告で87枚まで通る
  （declared≤150で無条件信頼のため。roomsSumとdeclaredの乖離検知>1.5で改善可） / SF-2 ai_estimate_verifiedは「外形寸法と整合」止まりで
  外形寸法自体の誤読は防げない（物理上限200でクリップされ暴走はしない・絶対上限100止まり）。
  coder報告の`__DISABLE_CEILING_GUARD=1`フックは実在しない（reviewerは手動パッチで代替検証・結果は有効）
**推定パス（展開図なし）の新築/リノベ分離（2026-07-25・coder/reviewer分離・差し戻し1往復・承認済み・未コミット）**:
とまるさんのAタイプ実測（壁PB 57枚・天井PB 48枚）が積算とズレた件の是正。**A〜F 6タイプ+派生4の正解を新規入手**。
- **【最重要】積算XLSは2ファイルに分かれている**: `①…A~F.XLS`＝A〜Fタイプ / `②…G.XLS`＝Gタイプのみ。
  従来②しか見ておらず「Aタイプの正解は存在しない」と誤認していた（②の「Ａタイプ」シートが中身Gという罠も②固有・①は本物のA）
- **集計表の列→タイプ対応に派生タイプが挟まる**（`B6`行の`{=現場名等!A*}`で確認）:
  `C=A E=A1 G=B I=B1 K=C M=C1 O=D Q=D1 S=E U=F`。**C/E/G/I/K/M=A/B/C/D/E/Fと読むと B以降が全部ズレる**
  （例: 壁PB B=111.91が正、123.10はA1。F=73.99＝Fは2LDK50.74㎡なので小さくて正）。戸数58(①)+9(②G)=67で実績表と一致
- **単位の取り違えに注意**: 集計表C列は**㎡**であって枚数ではない。Aタイプ 壁118.02㎡÷1.4=**84.3枚** / 天井61.007㎡÷1.45=**42.1枚**。
  「118枚・61枚」と読むと天井/床=1.23倍の物理矛盾が出る（正しくは0.88でGの0.90と整合）
- **リノベ係数0.6は根拠なしと確定**（`git log -S`で独立確認）: `de42b9b`で`GLASSWOOL_COVERAGE=0.5`（後にM-3で「根拠不明」と判定され撤去）
  と**同一コミット**に投入。当時のdiffに旧クランプ`[30,90]`が同居＝**帯に収める調整値**。しかも`1.37`は
  **アルファ67戸=新築実績**（6010/67/65.8=1.363）なので**新築実績×リノベ係数**というどちらでもない値だった
- **採用した構造**: `overrides.building_type`（'new'|'renovation'）で**外から指定**（推定は1箇所も無い・grep確認済み）。
  新築式は床面積比でなく**周長×階高ベース**（床面積比はアルファG 1.326枚/㎡ vs 別府0.486〜0.845＝**2.7倍差**で物件を跨げない。
  周長×階高なら別府0.434〜0.547+アルファG 0.614＝1.41倍に収束）。リノベは旧式を`WALL_PB_COEFF_RENOVATION`等に保存し**1枚も変えない**
- **天井PB控除は比率→絶対量モデルへ作り直し（差し戻し1回）**: 第1版は「補填分×0.42」で、
  **rooms:[]で控除率が床面積全体に掛かり最大-43%退行**（旧44→25枚）+**UB/収納の二重控除**（旧43→36枚）。
  reviewerが旧実装との22,701セル比較で発見。**replayが緑でも証明にならない**（実AI記録は14部屋拾えていてfill=1.81㎡しかなく
  控除0.76㎡が丸めに埋もれるだけ）。**天井行は`applyElevationTakeoff`にset()が0件＝展開図ありの本番経路でも常に推定パス由来**という事実も判明。
  第2版は**絶対量5.258㎡**（内法面積表−XLS天井PB実測の6タイプ平均4.51〜6.99㎡）+`clamp(5.258−ub−cl, 0, 補填分)`で二重控除解消+**新築オプトイン化**
- **答え合わせでない証拠**: 完全一致させるなら控除8.017㎡（誤差-0.2%）を選ぶところ、導出規則（6タイプ平均）を優先して**5.258㎡＝+4.6%を受け入れている**
- **結果**: Aタイプ`building_type=new` 壁PB80枚(-5.1%)✅ / 天井PB44枚(+4.6%)✅。
  リノベ既定はreviewer独自の**64,880シナリオ**（境界・極端値・override併用を追加）で旧実装と**完全一致**。
  ミューテーション5種で検出力も実証（新築ゲート撤去で45,101差分が発火）
- **新規eval**: `alpha-af-ground-truth.json`（10タイプ×10部位・XLSと100セル完全一致）+ `eval-alpha-af-truth.mjs` 567✅（エンジン非import）。
  **これで正解はG1つ→アルファ10+別府9=19タイプに**
- should-fix繰越: ①rooms:[]+新築で+14%過大（安全側だが警告未確認）②test-building-typeの±15%が控除有無を判別できない
  ③新築パスのミューテーション検知が1アサーション幅 ④層1発火時の`unlabeledFillArea=0`を守るテストが皆無（撤去しても全ゲート素通り）

**木胴縁の是正＋物件依存9件の全override化（2026-07-24・coder/reviewer分離・承認済み・未コミット）**:
「どの図面でも正しく出る＝アルファGタイプ専用ハードコードの解消」ゴールの後半。**eval 20✅/⏳0/✗0（初の⏳ゼロ）**。
- **木胴縁 -46.0%⏳ → -7.8%✅（A-7解消）**: 係数と拾い面積の**セット修正**。XLS原典 `集計表C85{='Ａタイプ'!P158+P212+P264+P321}`
  =界壁面5.047㎡/戸（洋室(3)Ｃ面0.98×2.45 + 台所Ｃ面1.08×2.45）、係数は`X86=0.0098`（`Y85{=W85*X$86}`）。
  **拾い面積過大の正体は「収納内RC面(r73→AK列)+EV面(r26→AJ26)+界壁面(r85→AJ85)を全部足していた」こと**（3行とも係数は同じ0.0098で、
  分離根拠は係数差でなく**集計先の行が違う**こと）。相殺の実証: 旧9.77㎡×0.002967=-46% / 係数だけ直すと9.77×0.0098=**+78%に反転** /
  両方直して5.047×0.0098=-7.9%✅。**この反転をテストで固定**（test-dobuchi-volume）
- **答え合わせでないことの証明**: `0.0537×9÷45.423=0.010640`（期待値から逆算した場合の係数）vs 採用値`0.0098`＝8.6%乖離。逆算なら一致するはず
- **eval閾値は緩めでなく厳格化**: 木胴縁2行を tol 50/**999（=判定放棄）**→ **10**。期待値0.0537は不変。
  HEAD値0.0432(-19.6%)は新tol=10なら✗で落ちる＝旧実装値を落とす強さ。旧注記「Gは界壁C04で胴縁なし」は誤りで、
  XLS原典に`B261/B318="界壁面" C="ＰＢｔ9.5+木胴縁"`が実在＝**胴縁ありが正**
- **物件依存9件をすべてoverride対応済みに**（check-engine-constantsの物件依存❌が0件）:
  下地高(一般/水回り)・界壁面(高さ/**面積**)・際根太(**規格/長さ係数/材積換算の有無**)・天井PB加算・遮音壁ルール。
  **際根太は単一定数では不可**（別府 際根太/床=0.97〜1.27 vs アルファ0.277＝3.5〜4.6倍差／別府はX9=0で材積換算せず）→
  `kiwaneta_ratio`/`kiwaneta_min_m`/`kiwaneta_spec`/`kiwaneta_volume`の**4項目1プロファイル**で解決。
  材積は「0を掛ける」でなく**行ごと出さない**（0m³行＝存在しない材の発注行になるため）。
  「規格だけ差し替えると-74%のまま」を**回帰テストとして固定**（過去2回のレビュー指摘をテスト化）
- **coderが自分に有利な変更を見送った例**: 際根太係数の真値は18.2÷65.76=0.2768で、採用すればevalが+4.4%→**-1.1%と良くなる**が、
  「今回の目的はoverride化であって既定値の精度変更ではない」としてceil境界をまたぐ変更を見送り。乖離は表に明示（+1.2%）
- **残る❌1件＝「スラブ下り際根太H=210」**（別府r10・戸当23.3m）は**定数でなく別府にしか存在しない部位行**。
  overrideでは原理的に埋まらないため別セクションに隔離して明示（隠さない）。**別府実運用にはこの行の実装が必要**
- should-fix繰越: SF-1 `UNIT_PRICES['際根太']=350`が名前キーのみでspec='H110'にも同単価が付く（表示スコープ外のため未到達）
  / SF-2 ratio=0+min=0で0m行が出る（volume='なし'と扱いを揃えるべき） / SF-3 wiringテスト2件目がnegative-only
  / SF-4 際根太に層3相当の絶対上限なし

**実績クランプ4件を3層ガードへ置換（2026-07-24・coder/reviewer分離・差し戻し3往復・4サイクル目で承認・未コミット）**:
アルファのリノベ実績帯クランプ（壁PB[30,90]枚/耐水[2,7]枚/巾木[30,60]m/間仕切GW[5,15]㎡）が別府9タイプで帯外
（GWは9/9・巾木7/9が帯外）になる問題。`check-engine-constants.mjs` の**クランプ判定❌0件を達成**。
- **採用した設計（3層）**: 層0=床面積が物理上限150㎡超なら150へ丸め`floor_area_implausible`（user_input経路は書き換えず警告のみ）
  / 層1=`totalFloorArea > sanityBase×1.25`で床面積そのものを按分縮小（部位の出力でなく**入力**を直すので全部位が同一基準になる）
  / 層3=**部位絶対上限**（壁PB120枚・巾木130m。別府実測max98.4枚/106.9m×1.22）。耐水・GWは床面積と独立な入力を持つため比率capが有効。
  `GLASSWOOL_COVERAGE`は`overrides.glasswool_coverage`でoverride化（既定0.135=アルファ基準・別府は0.5指定でMaterialResult.vueに入力欄あり）
- **差し戻し3回の内容（すべて「全ゲート緑」の状態で発見された）**:
  ①**M-2 置いたガードが構造的に発火しない**＝床面積比のcapを床面積比例の部位に掛けたため比が常に一定（540通りで発火0件）。
  「床面積比capは死ぬが絶対上限は床面積が伸びれば必ず到達する」＝**比と絶対値は別物**という区別が要る
  ②**M-4 cap撤去で2経路がHEADから回帰**（roomsSum=0またはdeclared=0のとき`sanityBase`が誤読値**自身**になり比≈1.0で層1が自己参照で無力化。
  declared=1000㎡で壁PB790枚が警告ゼロ。**専有面積は任意入力なのでゲスト運用の既定＋AI誤読だけで到達する**）
  ③**MF-1 「暴走0件」が格子サンプリングの偶然**（格子の到達上限119枚が閾値120のわずか1枚下だった。境界掃引すると
  decl=150/rooms=187で**155枚が完全無警告**、decl=151で124Wに落ちる非単調性も露出）
- **教訓（最重要）**: **格子の総当たりではなく境界の掃引で検証すること**。最終版はcoder 230,720件＋reviewer独立72万件＋28万件
  （別の刻み幅・未知のsource値・部屋数・天井高・layout_type軸）で暴走0件を確認。フォールト注入（cap無効化・実在物件を弾く値）で
  検出力も実証済み
- **際根太はスコープ外と確定**（reviewer独立検算）: 別府 際根太/床=0.97〜1.27 vs アルファG=0.277＝**3.5〜4.6倍差**。
  規格overrideだけでは数量-60〜76%のまま「対応済み」に見えて危険。`KIWANETA_RATIO`と材積換算の有無（別府X9=0）を含む**物件プロファイルが必要**
- **should-fix繰越**: SF-B 層3を持たない部位がすり抜け帯で無警告のまま実測max超（フローリング1.66倍・耐水PB2.84倍。**HEADからの劣化ではない**）
  / SF-C `wall_pb_sheets_small`は構造的に到達不能（代数証明+27万件で0発火）＝死にコード
- **天井PB加算のoverride化（2026-07-24・64c1a89・coder/reviewer分離・差し戻し1往復）**: +4枚はアルファG固有
  （集計表A74「ﾊﾟｳﾀﾞｰﾙｰﾑ・ﾄｲﾚ」C74=4。**別府はA74/C74が存在しない**＝reviewerが原典確認）なのに部屋名だけで無条件発火していた。
  `overrides.ceiling_pb_extra_sheets`（別府=0）で物件別設定可。**新規パーサ不要**（calculateMaterialsは既にoverridesを受け取る＝
  routes無変更で疎通。下地高はbuildupCalculatorへの受け渡しが要るため専用パーサが必要だった＝イディオムの違いを理解して使い分ける）。
  発火条件（パウダー/トイレ室の存在）は不変・アルファG出力はHEAD版とバイト単位一致（12ケース比較）。
  **サニティとの順序も検証済み**: 加算が先・抑制が後で、override=999999でも水増しブランチ40枚/絶対上限100枚に収束（overrideで暴走突破できない）
- **入力ガードの教訓（M-1）**: 旧実装は下地高overrideの`[^0-9]`除去を真似たが、**符号と小数点を消すため'-3'→+3(符号反転)・
  '2.5'→+25(+48%)が無警告で通った**。下地高は下流のSTUD_HEIGHT_MIN/MAX（2200-3200）が壊れた値を捨てる**第2段**があるので
  助かっていただけで、天井PBにはそれが無い。**パースの流儀だけ真似て安全網を真似ない**のが事故の型。
  許可パターン`/^(\d+)\s*枚?$/`+整数0〜20の範囲検証+不採用時は必ず警告、に是正（未設定時のみ無警告＝通常運用で鬱陶しくしない）
- **診断スクリプトの陳腐化（S-2）**: check-engine-constants.mjsが**撤去済みの天井PBクランプ[20,50]を未解決として報告し続けていた**
  （41155a2で比率サニティに置換済み）。行番号も7箇所stale。「解決済みを未解決として報告する診断」は次サイクルの判断を誤らせる。
  恒久策は行番号でなくgrepパターンでの動的解決（今回は実測値更新に留めた）
- **際根太は物件プロファイルが必要（実装見送り・原典確認済み）**: 別府はX9=0＝**材積換算していない**（mのまま発注）・規格H110・
  さらに**アルファに存在しない「ｽﾗﾌﾞ下り際根太H=210」行**（別府r10・戸当23.3m）が実在。単一定数overrideでは
  「材積行を出すか否か」「新部位行」を表現できず、**物件ごとの部位セット＝物件プロファイル**の導入が筋。
  なお別府のKIWANETA_RATIO=0.28推定は別府H(107.9㎡)→30.2mで正解136.9mに対し-78%（展開図なしパス専用の推定・実測経路は+4.4%✅）
- **A-4是正済み（2026-07-24・5bdba56）**: EV廻り壁PB換算を÷1.4→専用定数EV_WALL_PB_SQM_PER_SHEET=1.5（XLS X62・両物件一致）に分離。
  壁/耐水/遮音の÷1.4は不変。ceil丸めでG実測2.16㎡は2枚で不変だが大面積タイプで是正。方向注意=÷1.5は枚数を減らす（1.4流用は過大側だった）。
  他の1.5系（一部界壁3枚/収納面5枚）は固定枚数ハードコードで換算せず＝÷1.4誤用なし。別府の界壁面積換算（X30/X73=1.5）未反映は申し送り

**Gタイプ突合検証（2026-07-10・page_45拡大読み+page_56展開図で実施）**:

**実装済み（第1段）**: buildupCalculator.js（部屋×A〜D面×部位のボトムアップ集計+壁仕上記号の振り分け）、
ELEVATION_PROMPT/DOOR_SCHEDULE_PROMPT+analyzeAuxDrawing（Claude単体）、upload APIのfields化
（file必須+elevation/door_schedule任意・補助図面は抽出後に即削除）、calculateでの実測置換
（applyElevationTakeoff）、Home3スロットUI（①平面詳細図=床天井編/②展開図=壁編/③建具表=建具編）。
**表示スコープ（一旦）**: 資材リストは建材14項目のみ表示（filterKenzaiScope。ユーザー指定）。
キッチンパネルはキョーライト アーバンSマリアパールへ仕様変更済み。
**表記統一（2026-07-15）**: 建材14項目の名称・摘要は「やまがみ様【見積明細内訳】.xlsx 木工事シート」
（=A〜H全タイプ67戸の総量。名称/摘要の正）と同一表記に統一（例: 名称「壁 石膏ボード」摘要「t-9.5（3'×6'）」。
3'×6'=サブロク判=910×1820mm）。列ヘッダも資材名/規格→名称/摘要へ変更（表示・Excel出力とも）。
applyElevationTakeoffのマッチャは新名称の完全一致（間仕切GWがEV廻りGWにも二重書込みされる潜在バグも同時修正）。
データ対応: 見積明細の木工事シート=67戸総量 ↔ ②木及び建材XLSの集計表=タイプ別内訳（Gタイプ=戸当×9戸。表の「10戸」は誤り）。
**Gタイプ突合検証（2026-07-10・page_45拡大読み+page_56展開図で実施）**:
- PB合計（壁+遮音+耐水）: エンジン143.32㎡ vs XLS 141.49㎡ = **+1.3%** ← ボトムアップ方式の妥当性確定
- 木製巾木: 62.2m vs 56.44m（玄関樹脂分3.75と掃出し下見切り2.4を分離すれば±3.5%以内）
- 記号割付の確定（page_45拡大タイル読み取り）: 外周壁=I14 / **界壁・柱型=C04（打放・PBなし・クロス直）** /
  クロゼット内=D64（コンパネ） / EV側=D14 / 間仕切=G14 / 水回りも通常PB（耐水はUB廻りのみ）
- 残課題: 遮音壁L/O面の特定（3.48 vs 12.98。総量には含まれており内訳の問題）、
  耐水PBのUB廻り限定、巾木の樹脂/木製の部屋内分離
- 画像拡大はPowerShell+System.Drawingでクロップ×3拡大（scratchpad/g45_*.png方式）が有効

**Gタイプ全部位eval（scripts/eval-gtype-buildup.mjs・2026-07-10）**: 11部位中10部位✅（±10%以内・✗ゼロ）。
壁PB+9.4%/耐水-1.9%/巾木+10.2%/クロゼット内RC面-0.4%/際根太+4.4%/フローリング+6.6%/置床+7.6%/
置床上合板-7.5%/天井-2.3%/キッチンパネル-15.4%。⏳遮音壁・間仕切GW（UB裏面が展開図外・部分計上）と
間仕切下地（材積換算層とセットで実装予定）。修正したバグ: 置床にUB床面積が混入（+130%→+7.6%）、
収納床がフローリングに混入、天井除外の「クロゼット」表記ゆれ。

**タイル分割解析（2026-07-15実装・E2E実測済み）**: A1図面はAI送信時に縮小され記号・建具が潰れるため、
sharpで3×2分割+拡大した詳細パスを追加（drawingTiles.js + analyzeWallCodesTiled/analyzeOpeningsTiled。
展開図アップロード時に並列実行・Claude12回/約数十円）。実測E2E: 壁記号0→10部屋、開口6→23件、キッチンCH誤読解消。
**残課題**: ①開口の寸法null → **2026-07-15にマッチング層で対応済み**（下記「開口寸法マッチング層」。実E2Eでの効果確認は未）
②記号の部屋帰属がタイル境界で誤る（隣室のD64がトイレに付く等）→ 全面適用は標準PB系(G/I/L/O/W/S×中間1/2/4/5)に限定済み。
C04（界壁PBなし）の面単位割付は幾何情報が必要で未解決。**壁PBの実AI読みは+81%**（正解88枚→出力159枚。
記号を面に割り付けられず全面PB扱いになるのが本丸。手修正した記号割付なら+9.4%=96枚）。

**開口寸法マッチング層（2026-07-15実装・coder/reviewer分離レビュー承認済み）**: buildupCalculator.jsに
normalizeDoorSymbol / buildDoorLookup / resolveOpening を新設。①符号の正規化マッチ（全角→半角・大文字化・
ハイフン類/長音除去。同符号で寸法矛盾なら符号マッチ拒否=安全側）②寸法欠落開口のみ推定マッチ
（type分類→取付位置→寸法帯 幅±30/高さ±15mm。候補間で一意な寸法だけ補完・矛盾はnull維持・matched_by:'inferred'の印）。
玄関/SD/鋼製は推定対象外（符号マッチは可）。取付位置照合は候補1件時も適用。転記済み寸法は上書きしない。
takeoffに opening_match 統計{symbol, inferred, unresolved}。プロンプトにも建具符号の転記要求を追加（転記のみ・寸法推測なし）。
ユニット37✅（scripts/test-opening-match.mjs）・eval 17✅/✗0回帰なし。**実E2E（実AI読取での符号読取率）は未検証**。

**C04面割付（2026-07-15実装・長辺/短辺マッチ）**: タイル読取に「記号が付く壁＝部屋の長辺/短辺」を追加し、
buildupが面幅の長短で面に割付（面記号>割付>部屋単一記号>デフォルトG14。A〜D面の向きは作図者次第のため方角は使わない。
ほぼ正方形の部屋はスキップ）。本番E2E実測: **壁PB 155枚→128枚**（正解88・手動割付なら96=+9.4%）。
残る差の原因: ①記号読取の回間変動（wall_code_rooms 6〜10部屋/回）②同一記号の複数壁対応
（analyzeWallCodesTiledのdedupeが部屋×記号で1件のため、C04が長辺と短辺両方にある部屋は片方しか除外されない）
③本番のRenderキー失効騒動はキー差し替えで解決済み（エラーに原因コード401/429等を表示する改修も実施）。
摘要は「t-9.5（3'×6'）910×1820mm」形式（mm併記）に変更。

**寸法mmマッチへの再設計（2026-07-15・643d5cf）**: 長辺/短辺の幾何判断は誤りやすい（LDK・キッチンで誤読を確認）ため、
タイル読取を「記号＋その壁の寸法mm転記」に変更し、展開図の面幅と±80mmでマッチ（buildupのplacementByFace）。
プロンプトに業務知識を注入（C04は窓のあるバルコニー側にも付く／1部屋に記号2〜4個／G24=UB廻り・L14=遮音）。
耐水（中間2/5）の部屋単一記号の全面適用を停止（トイレ全面耐水の暴発を修正）。
**検証の範囲に注意（自レビュー済み）**: リプレイ（scripts/recordings/gtype-vscode-claude-read.json→replay-gtype.mjs）で
壁PB95枚(+8%)/耐水5枚(0%)/天井42枚(0%)だが、これは**エンジン+割付層の検証**。記録の記号・寸法は正解既知で構成しており、
①実AIがこの読み質を出せるか ②C04面の壁寸法が平面図に寸法線として実在するか（±80mmは芯々/内法差を未検証のまま仮定）
③G24の小さい楕円を実AIが拾えるか、は**実AI E2Eで未検証**。クレジット復活後のE2Eが最終判定。
※操作手順の「時計回り」はXLS入力者のルールであり、図面のA〜D面ラベル規則とは別問題（混同しないこと）。

**段階式アップロード（2026-07-15実装・実AIテスト済み）**: Homeを3スロット同時→STEP1〜3のウィザードに変更。
POST /api/projects/:id/aux（elevation|door_scheduleをどちらか1枚）を追加し、①平面図の解析結果（部屋一覧）を
②③のプロンプトに渡す（roomContextNote。部屋名の表記ゆれ・読み落とし対策）。展開図は再アップロードで丸ごと差し替え、
**建具表は符号単位マージで複数ページ対応**（③解決。実測: page_61=SD/AWD 15件 + page_63=WD 22件 → 37件、既存符号保持）。
展開図マージ処理はattachElevationData()に共通化（一括uploadと両用）。
図面ページの訂正: **WD木製建具表=page_63**（page_62はアルミ建具表AW/AD、page_64は建具表でない）。

**未実装**: 実図面での読み取り検証（Gタイプ展開図で正解突合）、
記号の平面図読み取りプロンプト強化（現状は展開図読取に記号が含まれない場合デフォルトG14扱い）、
**遮音シートの資材行**（takeoffは `sound_sheet_sqm` を拾うが対応する資材行が無く出力に出ない。
理由: 発注用の独立行が拾い出しXLS・見積明細のどちらにも存在しない＝名称/摘要/単価の正が無い。
実測: 別府 集計表54行「遮音壁ＰＢ張り」の規格は "t9.5+GW" でシートを含まず、シートは30行
「EV面遮音壁（界壁）PBt9.5+9.5GW+ｼｰﾄ+木胴縁」・34行の**複合行の規格文字列の中にしか出ない**。
アルファ見積明細にも遮音シート行なし。現状は拾った面積を `_warnings`（field:'sound_sheet'）で
明示し手動追加を促す。行を新設するならけいとさんに名称・摘要・単位・換算係数を確認してから）。

**造作材の材積発注（2026-07-15実装・coder/reviewer分離レビュー済み）**: timberVolume.js（断面×長さ×本数の材積換算層）
+材積4行（際根太/間仕切木軸/木胴縁（界壁面）/天井下地、単位m³・単価未整備¥0）。XLS原本の数式解読で
**「間仕切下地(木)84.082m」の実態は㎡**（Σ間仕切壁幅×下地高−開口。下地高=天井高+370mm=スラブ下端まで）と確定。
エンジン換算: Σ面拾い÷2（壁1枚換算）+UB隣接鏡像（開口控除済）+収納内側3×√面積推定（部屋名正規化でdedup）→G実測-7.7%✅。
材積係数: 間仕切木軸=拾い㎡×8.89m/㎡（縦横@450両面）、天井=野縁@303格子+吊木@910、胴縁=RC面×@455。
eval 17✅/⏳3/✗0（既存10部位悪化ゼロ）。木胴縁の部分実測疑い（実測<推定50%）は_warningsへ配線済み
（source:'calculate'で再計算時置換・AiReading.parsedDataへマージ）。
**要ユーザー判断**: 材積4行はfilterKenzaiScope（建材14項目）外のためAPI/Excel/画面に出ない（データ層のみ）。
表示するなら木材ブロックのスコープ追加が必要。8.89m/㎡の解釈はけいとさんに確認推奨。

**壁PBのプロXLS方式整合（2026-07-15・coder/eval/reviewerサイクルで実施・未コミット）**: XLSタイプ別シートの
行レベル照合で差分の3系統を特定し整合。①壁ボード類の拾い高さ=**CH+40mm**（WALL_PICKUP_EXTRA_MM。XLS実測4.84×2.24/3.64×2.44）
②D下地×中間1/4を壁PBから分離→**ev_wall_pb_sqm**新設（「EV廻り壁 石膏ボード」実測置換。Gのみ検証・他タイプの防露D14も
積まれる近似は要注意コメント済み）③玄関・廊下の廊下ドア開口の転記漏れ補完（page_56クロップで面帰属確認: B面=WD-12C/2TA/3TB/8B・
D面=WD-1TA850のみ）④収納内側下地を**opts.closetInteriors実寸**（家具工事シート: WIC コ型4680mm/CL2 2470/CL3 2620）で
置換可能に（3×√面積はフォールバック。D6*控除は推定分のみ=XLSに収納控除行が無い実証に基づく）。
**結果: eval 17✅/⏳3/✗0**（壁PB+7.2%・巾木+7.2%・間仕切下地-5.3%・耐水-0.2%）、**リプレイ壁PB93枚(+6%)/耐水5枚/天井42枚**。
XLSの間仕切下地84.082=6室ブロック合計と完全一致を実証。残る壁PB+7.2%の主因: XLS控除3.75mのうち図面転記可能分2.35m
（差1.40m≒洋室(1)ドア0.80が展開図D面に未描画+未特定0.60。けいとさん確認が確実）。

**資料の全数読み（2026-07-15・ユーザー指摘による棚卸しで新事実）**:
- **XLS操作手順シート: 「Ａ〜Ｄ面は右廻り（時計回り）で入力する」**←動画の「ルールなし・作図者次第」は誤り。
  面割付の幾何情報として利用可能（現状の寸法mmマッチで+8%達成済みのため未使用・強化の切り札）
- **特記仕様書(2)=意匠図page_05（hires有）**: PB9.5/12.5・防水耐水PB9.5・GW24kg/m³t50=「住戸内遮音壁」・
  硬質ウレタン吹付A種1H 20/30mm・木製巾木H40=樹脂シート貼・複合フローリングLL-45・二重床（水廻り）。
  動画でとまるさんが探して見つからなかった仕様書はこれ。page_06=特記仕様書(3)（断熱厚）、page_12=建築概要・面積表（タイプ別戸数の正）
- **見積明細 家具工事シート: タイプ別の収納固定棚実寸**（Gタイプ洋室1 WIC=コ型W(1140+2400+1140)×D500、
  洋室2 CL W800×D835等）→収納内側の「3×√面積」推定を実寸置換できるデータ
- 見積明細 内装工事: 下地合板T9.0=323㎡/乾式置床H200(FUKUVIフリーフロアSDG)=251㎡/床シート下合板=220㎡（67戸・既記録と一致）
- 木製建具工事: WD-01は「(1135+1135)×2170 2枚引込み+2枚引込み戸」（上の67戸表の2270×2170 4枚引込みは合算表記）
- けんとまとめ/ = リノベ実績写真39枚（新築3図面スコープ外とユーザー確認済み）

**打ち合わせ議事メモ（2026-07-14通話・録画2本を文字起こし済み）**: `docs/meeting-notes/2026-07-14-kabe-hen.md`。
要点: 換算はXLS X列の係数をそのまま使う（壁PB÷1.4=87〜88枚が正解。1.6562はロスなし理論値）、
比較対象は集計表の戸当列、ゴールは**タイプごとの見積書**（現場名「〜Aタイプ」運用）、
展開図のA〜D面の向きは作図者次第でルールなし、マイルストーンは「壁が完璧になったら次のミーティングで床編以降を聞く」。
これを受け天井PBの推定も÷1.4に変更済み（2026-07-15）。備考欄は低優先で未実装。

**集計表シートの実係数と発注列の構造（2026-07-16・ユーザー指摘で全行ダンプして確定）**:
- 換算係数X列は**部位で異なる**: 壁・耐水=1.4 / 天井・下り天井・収納面(ｸﾛｾﾞｯﾄ内RC面)=**1.45** / 一部界壁・W下地=**1.5**。
  「全部÷1.4」は誤り（天井は59.087÷1.45+ﾊﾟｳﾀﾞｰ･ﾄｲﾚ調整7枚=AD列373.749÷9戸=41.5枚/戸。エンジン42枚出力は結果一致のため係数は据え置き・要検討）
- **壁-PBt9.5の発注列(AB)=102.4枚/戸** = 一般壁87.2（122.061÷1.4）+ 遮音壁9.3（12.979÷1.4）+ **W下地6.0（8.995㎡÷1.5・ボード共スラブ下まで）** の合算。
  エンジンは遮音壁を別行で持つため「壁 石膏ボード」の比較対象は一般分87.2枚
- Gタイプの一部界壁耐水PB（AA列）=**0**（エンジンの推定1枚は過大・±1枚判定内）
- **W下地重複仮説は棄却（2026-07-16検証済み）**: 8.995㎡の正体はＡタイプシート押入ブロックの**【EV面グラスウール】3.5m×2.57m**
  （=W下地のEV面。PB÷1.5+GW+木軸0.79m³を1行から発生）。エンジンのwall_pbには含まれず重複なし。
  なおXLSのEV面はPB6.0枚/戸+GW8.995㎡/戸だが、見積明細67戸平均（EV廻りPB2.2枚・GW2.1㎡）とは大差
  →EV隣接住戸のみ計上の可能性があり、エンジンの実測2枚/推定2㎡をXLS値に合わせるのは早計（要けいとさん確認）
- **壁PB残差+7.24㎡の部屋別分解（単室takeoff実測 vs Ａタイプシート部屋ブロック）**:
  洋室(3) **+5.03**／キッチン+LDK **+3.73**／洋室(1) +0.95／トイレ +0.79／パウダー +0.49／
  玄関・廊下 **−1.85**／洋室(2) **−1.91**（正負が相殺して+7.24）。
  「玄関の控除1.40m」は主因ではない（玄関はむしろエンジンが少ない）。
  最有力: 洋室(3)・LDK系で**C04面の寸法マッチ失敗→全面PB扱い**（洋室(3)のXLSはPB壁3枚=1.24+3.24+3.33mのみ。
  エンジン+5.03≒C04面2.36m×2.44=5.76に近い）。次サイクルは部屋別差分の面単位特定から
- replay-gtype.mjsの合格判定を集計表ベースの10項目（✅8/⏳2）に拡張、`docs/TESTING.md`（テスト手順書・Copilot用）と`scripts/dump-ui-gtype.mjs`（UI相当出力+summary整合チェック）を新設（2026-07-16）

**期待値の数式監査（2026-07-16・「エクセルの計算式がすべて」の指摘で数式セルまで抽出して照合）**:
- **eval期待値2件が答え合わせだった→是正**: ①キッチンパネル面6.7㎡はXLSに存在しない値
  （実セル='Ａタイプ'!P313=3.925 → エンジン5.77は**+47%✗が真の姿**・現状の既知バグ）
  ②フローリング53.26=48.42×エンジンロス1.1の循環比較 → ロス前48.415 vs floor_area 51.59=+6.6%✅に是正。
  **eval現状: 16✅/⏳3/✗1（exit 1）**。✗はKP修正まで容認、増えたら回帰
- **耐水PB6.45の一致は偶然**: XLS C58=台所ブロックのUB廻り壁のみ（便所P376・洗面P430=0、
  トイレ・パウダーの壁は通常PB扱い）。エンジンはトイレ950/パウダー1925のG24面を耐水計上。
  数値は一致するが拾っている壁が違う（壁PBの部屋別構成ズレの一因）
- **遮音壁⏳の真因は3つ**: UB裏面が展開図外＋洋3(1.05m)・台所(1.45+1.1m)の記号読み漏れ＋拾い高さ差
  （XLSは下地高2.57、エンジンはCH+40=2.44。洋1でXLS3.726 vs エンジン3.54）
- **天井も式は不一致**（XLS÷1.45+ﾊﾟｳﾀﾞｰ手動+7枚 vs エンジン÷1.4。42枚は偶然一致）
- **壁PBの幅の拾い方が構造的に違う**: XLS=平面の壁セグメント単位（洋3: 1.24+3.24+3.33m）、
  エンジン=展開図の面幅（2.36+4.14+3.29m）。C04面除外がplacement1件分しか効かない部屋で過大（洋3+5.03の主因）。
  開口控除高さも違う（XLS洋1折戸1.2×2.0 vs エンジン建具表2320）
- XLSシート構造の確定: タイプ別=F(D×E)+I+L+O→P小計→部位計P=加算−控除→R=P×戸数（現場名等!B4=9）。
  集計表C列=タイプ別シートPセルの直接参照合算（行対応が完全に特定可能）

**ロジックミス修正サイクル1（2026-07-16・coder/eval/reviewer分離・レビュー承認済み・未コミット）**:
- **KPカウンター帯モデル**: 表面6の拾いを面全面(5.77㎡)→「面幅×max(0,CH−0.85)−開口」(3.48㎡)に修正
  （XLS式'Ａタイプ'!P313=2.5×1.35+0.7×2.2×2−2.2×1.15=3.925。袖壁+3.08と吊戸控除−2.53は未モデル・ほぼ相殺）。
  KITCHEN_COUNTER_H_M=0.85/KP_SHEET_SQM=2.2022(0.91×2.42実板寸)新設。applyElevationTakeoffにKP実測置換+summary同期追加
- **天井PB式のXLS化**: ÷1.4 → **(天井面積−パウダー・トイレ天井)÷1.45+4枚**（集計表X77=1.45・74行の4枚/戸調整。
  G検算41.5枚一致・小部屋を特定できない場合は÷1.45のみで+4は暴発しない）
- 結果: **eval 17✅/⏳3/✗0（KP -11.3%✅）・replay ✅8/✗0（天井41枚/KP3枚）・ユニット37✅**・回帰ゼロ
- should-fix繰越: ①summaryとKP/EV資材行の不整合（実測0時にsummary=0/資材行=推定値。読者不在のため実害なし・次サイクルで書き戻し方式に統一）
  ②KP帯からの開口控除が帯外開口で過大側（0止まりガードあり）③replayのKP行は[推定]のまま（記録に表面6記号なし・実AI読み課題と同根）
- **未修正のロジック課題（次サイクル以降）**: 壁PBの幅拾い構造差（平面セグメントvs面幅・洋3+5.03/LDK+3.73）、
  遮音壁の高さ2.57と対象壁の読み漏れ、耐水バケット構成差（トイレ/パウダーG24 vs XLSは台所UB廻りのみ・合計偶然一致）、
  Gタイプ定数の推定行（下り天井4・一部界壁3・EV GW2等）の数式化、EV廻り÷1.4vs W下地÷1.5

**AIプロバイダ切替+Gemini実読みE2E（2026-07-17）**: `AI_PROVIDER`環境変数（dual/gemini/claude・デフォルトdual=現行不変）、
`GEMINI_MODEL`（デフォルトgemini-2.5-flash）、`GEMINI_RETRY_MAX`（デフォルト0・E2Eスクリプトのみ4）。
scripts/e2e-gemini.mjs=Gタイプ5図面を本番と同一の組み立て（analyzeDrawing→attachElevationData→mergeDoorSchedule）に通し記録保存→replay。
**Gemini 2.5 flash実読み結果: ✅7/✗1/⏳2**（壁PB82枚=-6%・天井42枚・KP3枚。✗=耐水3枚-35%。
壁記号14部屋・建具48件・展開図8室を読取。実AI読みで初の合格圏＝Claude実読みの壁PB+81%より大幅に良い）。
gemini-2.5-proは無料枠quota=0で未測定（Billing有効化後に `node scripts/e2e-gemini.mjs --model gemini-2.5-pro`）。
本番をGemini化するにはRenderに `AI_PROVIDER=gemini` + `GEMINI_RETRY_MAX=4` を設定（12並列タイルが無料枠RPMで429必至のため）。
残課題: 耐水-35%（G24読取3枚どまり）・LDKのC04過剰検出（13placements）。ローカル.envのGeminiキーがセッションログに露出→ローテーション推奨。

**Gemini読み対応サイクル（2026-07-17・coder/eval/reviewer分離・承認済み）**: 上記残課題への対応。
- **耐水の第2パス救済マッチ**: 耐水記号（中間2/5）×水回り部屋名（WET_ROOM_NAME_RE）限定で、±80mm未割付の
  placementをtol=300mm（両端壁厚合計の物理上限）まで緩めて未割付面へ割付。Geminiリプレイ耐水3→4枚✅・
  壁80枚(-8%)・Claude記録/eval/ユニットは完全不変（第2パス非発動）。副作用: 間仕切下地75.5→77.1m（鏡像加算・正解方向）
- **プロンプト改善（実AI E2E未検証・明日の1発で確認）**: ①展開図の分割寸法合算（1,200+3,990→5,190。洋室(1)の-6%対策）
  ②タイルの器具寸法除外＋「同じ記号は1部屋最大4件」（部屋全体の上限はレビュー指摘で撤回=正当なC04×4+I14を落とさせない）
- **運用方針（ユーザー決定）**: Gemini無料枠=flash 1日20リクエスト≒アップロード1回分。**毎日1回の実読みE2E→修正のサイクル**で
  精度を詰め、安定後にBilling課金を判断。今日の枠は消費済み（1回目E2E+2回目が日次上限で中断・quota切れは読取欠落として顕在化する教訓）
- 次の1発の手順: `cd backend-node; node scripts/e2e-gemini.mjs` → 記録が保存されreplayが自動実行。
  ブラウザでやる場合はRenderに AI_PROVIDER=gemini / GEMINI_RETRY_MAX=4 を設定してから（2026-07-17設定済み・本番Gemini稼働中）

**Gemini課金化+モデル調査+タイルスキップ穴の修正（2026-07-19・coder/reviewer分離・承認済み・09e5dcb）**:
- **課金有効化**（¥2,000前払い・Tier1・オートチャージOFF・検証済み）: 日次20リクエスト制限が消滅。
  フル解析1回≈10円（2.5-flash）。1日1回サイクル→随時実験可能に
- **モデル一覧を実測**: gemini-2.5-proは新規ユーザー廃止(404)。**gemini-3.5-flash等の新世代が利用可能**
  （3-pro/3.1-pro-preview/3.5-flash他。ListModels APIで確認）
- **3.5-flash試験**: 読み質は歴代最高（展開図面幅を全問正解・平面図警告0・記号ジャンクなし・STEP1で28.5m素通し）。
  ただし①STEP1が部屋別codes（placementsなし）を返す→タイル解析スキップ条件の穴で面割付全滅=壁PB148枚+70%
  ②開口タイルの符号を別形式（G6/03等）で転記し建具表と0件マッチ→開口控除ゼロ（opening_match unresolved:35）
- **①の穴は修正済み（09e5dcb）**: wallCodesNeedTileReread（寸法付きplacementが1件も無ければ再実行）+
  mergeWallFinishCodes（タイル結果を正に部屋名ゆれ突合で上書き・未読部屋は保持）。ユニット15✅新設・全ゲート緑
- **②が3.5-flash採用の残る障壁**（次サイクル本命）: ELEV_OPENING_TILE_PROMPTの符号転記強化 or 姿図番号→符号対応層。
  レビュー指摘: 開口控除だけでなくC04面割付の±80mm不一致も併存（差85.8㎡の内訳を閉じないこと）。
  検証素材は scripts/recordings/diagnostic/（3.5記録・2.5の耐水3枚ブレサンプル）
- **回間ブレの実測**: 2.5-flashは同一図面で壁PB 75枚（ブラウザ）↔92枚（直後のE2E）。耐水も3〜9枚で振れる。
  ブレ根治の選択肢=3.5-flash化（②解決後）or 複数回読み多数決（課金で可能に）
- レビューshould-fix繰越: STEP1がplacements付きwall_finish_codesを幻覚するとスキップ判定を素通りする残存穴
  （validatorでplacements剥がし推奨）・merge包含突合の「洋室(1)クロゼット」型テスト追加
- **ブラウザ実測で初の合格（2026-07-19・09e5dcbデプロイ後・2.5-flash）**: **壁 石膏ボード90枚(+3%)**・耐水4枚・天井42枚・
  KP3枚。ガード発動も正常（AWD-102幻覚2件除外・開口90%縮退3面）。残✗は遮音壁0.22㎡（既知・LDK隣接数式化で解消予定）。
  ブラウザ推移: 73→75→**90枚**。「壁が完璧になったら次のミーティングで床編」のマイルストーンに接近

**遮音壁の数式化+下地高是正サイクル（2026-07-19・coder/eval/reviewer分離・差し戻し1往復・承認済み）**:
- **DEFAULT_SOUND_WALL_PAIRS**（LDK↔洋1 1450mm/LDK↔洋3 1050mm・出典'Ａタイプ'!P113/P221/P275）: 記号読みに
  頼らない宣言的ルールで遮音壁を計上（PB両面12.85㎡・GW片面6.43㎡・高さ2.57）。両部屋存在ゲート+部屋ごと±80mmで
  最大1面消費（C04/D64/G24は消費しない）+L/O/W placementの遮音採用は**ペア構成部屋限定**（玄関L14@1000ノイズ対策・差し戻しで追加）。
  opts.soundWallRuleで差し替え可（他タイプはGデフォルトが誤発火しうる=should-fix②の警告表示は**2026-07-24に実装済み**。
  下記「別府記号レビュー指摘の修正サイクル」参照）
- **下地高の是正（A-2/A-3解消）**: STUD_PLENUM_M(CH+370)廃止→**2.57固定/水回り(WET_ROOM_NAME_RE)2.77**
  （出典E123居室/H339水回り）。L14/O14面の遮音高さも2.57/2.77に。**S6（水回り無控除仮説）はダンプで棄却確定**
  （便所P341・洗面P395に控除行実在）→間仕切下地は-5.3%→**-7.8%**（相殺誤差の解消による正直な悪化・±10%内。
  残差の正体=洗面のUB囲い壁4.54m@2.77等が展開図に写らない構造差+台所r284のJ284=1.05×2.57の遮音木軸疑い）
- **結果: eval 19✅/⏳1/✗0**（遮音壁-1.0%✅・GW+0.1%✅が⏳から昇格）・**replay3記録✅10/✗0**（判定対象10項目化）・
  placement 48✅・全ゲート緑。レビュアーがXLS原典セル一致まで独立確認（答え合わせ成分なし）
- should-fix繰越: ②デフォルト遮音ルール発火時の_warnings表示（他タイプ誤発火の可視化）③ペア消費の部屋横断キャップ
  （LDK_LIKE_RE複数部屋で最大3面消費しうる）・残る⏳1=木胴縁適用後-46%（A-7係数とセットで次サイクル）

**タイル安定化+Home状態永続化サイクル（2026-07-20・coder/reviewer分離・差し戻し1往復・承認済み）**: 本番ブラウザで
「壁記号タイル1〜2/6件失敗→壁PB98〜108枚(+12〜24%)」が毎回発生（課金Tier1でも12並列一斉でRPMバースト）への対応。
- **analyzeTilesの3並列プール+ジッター100〜300ms+失敗タイル第2スイープ**（全完了後5秒待って失敗分だけ直列再試行・
  スイープ呼び出しはretryMax:0で二重リトライ禁止=最悪でも+3分上限）。test-tile-pool.mjs 13✅新設
- **Homeのアップロード状態をsessionStorageに永続化**: 結果画面から戻ってもSTEP1〜3・現場名・専有面積が復元
  （リロード後はGET /projects/:idで復元）。「↺新しい現場を開始」ボタン新設。「戻ったら全部消えてる」問題の解消
- 全ゲート緑（eval 19✅/replay3記録✅10/✗0・ユニット計159✅・build成功）。実AIでの並列絞り効果は次回ブラウザで確認
- 混入検査: materialCalculator.jsに並行セッション由来の未レビュー変更（従来パスisWindowのsymbol対応）を発見→
  **コミットから除外**（buildup側isWindowとの二重実装・判定不一致あり。共通化+テスト付きで別サイクル再提出が筋）

**429の真因=Geminiプリペイド残高枯渇（2026-07-20・当初RPM誤診→訂正）**:
本番429の真因は**「Your prepayment credits are depleted」=Gemini前払い残高0円**（単発リクエストのerrorメッセージで確定）。
7/18入金¥2,000が7/1〜20で¥2,104消費し残高-¥104。RPM制限でも多数決集中でもなかった。
- **誤診の経緯（教訓）**: 「20並列テスト全通過→Tier1・課金枠OK」と判定したが、それは残高が残っていた時点の話。
  その後の開発テストで残高を使い切り「Tier1キーだが残高ゼロ」に。**429はRPMだけでなくクレジット枯渇でも返る**（要区別）。
- **¥2,000が2日で溶けた主因**: ①多数決3回読み=1人24回（通常の3倍）②GEMINI_RETRY_MAX=4で429リトライ再投擲が積み上がる
  ③開発中のローカルE2E（Claude単独/dual/20並列/各疎通）が本番と同じキーを叩いてユーザー残高を消費（本番枠を食う）
  ④ブラウザ複数回アップロード（73→75→90→108→…→dual→今回）。多数決+リトライ4の組合せが破壊的。
- **対応**: WALL_CODE_READS_GEMINI=1（設定済み）+ **GEMINI_RETRY_MAX 4→2**（リトライ再投擲抑制）+ Geminiクレジット追加。
  これで1回読み+リトライ2＝フル解析1回≈10円に戻る（¥2,000で200回）。
- **恒久対策（コード・次サイクル）**: ①GEMINI_RETRY_MAXデフォルト値を下げる②429が「credits depleted」のときは
  リトライせず即「運営者に連絡（残高切れ）」の専用エラーを出す（RPM 429の再試行誘導と区別・auxAiErrorResponse系に追加）
  ③開発中はローカルでGeminiを叩かない（ブラウザ優先の徹底・本番残高を食わない）。
- 旧説（RPM主因・多数決集中が429の直接原因）は誤り。多数決集中は消費速度を上げた要因ではあるが、429自体は残高0が直因。

**429対応=Gemini単独は多数決1回読み・複数人運用の制約確定（2026-07-20・上記で真因訂正済み）**:
本番ブラウザで「AI解析が一時的に失敗（429）」+解析10分。原因調査でキーは課金Tier1前払い（20並列テスト全通過で確定・
末尾B4vw・gen-lang-client-0145770912）と判明→429はキー不足でなく**多数決3回読み=1人24回のGemini呼び出し集中**が主因（←訂正: 真因は残高枯渇。上節参照）。
- **設計上の重大制約（ユーザー指摘）**: 全ユーザーが1つのGOOGLE_GEMINI_API_KEYを共有。1人24回×同時利用でTier1(150-300RPM)を
  瞬間的に超え、複数人が同時に使えない。多数決3回読みはGemini単独では「同じAIの同じ読みを3回」で精度効果薄く、
  遅さ・429・複数人不可のデメリットのみ（dual二人読み自体も精度悪化で棄却済み）。
- **対応: WALL_CODE_READS_GEMINI=1 をRenderに設定**（1人8回→開口込み12回・解析1〜2分・同時十数人OK）。
  今日のブラウザ実測90枚(+3%)は多数決前の1回読みの数字＝精度は据え置き。多数決コード自体は残す（将来用）。
- **教訓（要注意）**: 開発中にローカルE2E（Claude単独/dual/20並列テスト）が本番と同じキーのRPMを食い、ユーザーのブラウザ枠を
  奪って429を誘発しうる。ブラウザテスト中はローカルでGeminiキーをApiに叩かないこと。
- **未解決の本格対応（複数人運用の本命・次サイクル）**: ①解析リクエストのキュー化（RPM上限内で順次処理）②会社ごとにキー分離
  ③キーローテーション（末尾B4vwはセッションログ露出・要作り直し）。当面は1回読みでRPM余裕を確保して凌ぐ。

**AIプロバイダ選定を実測で確定=Gemini単独が最良（2026-07-20・Claude課金後の3構成直接比較）**:
ユーザーが「AIに2つ読ませて精度を上げる（Gemini3回+Claude3回で決める）」ためClaude APIに$22課金。dual二人読みを実装し実AI検証。
- **temperature 400バグ発見・修正（5d34c1d・本番Claude経路の障害）**: opus-4.7/4.8系・sonnet-5・fable-5はtemperature廃止で
  400 "temperature is deprecated for this model"。dual実読みで本番Claude経路が毎回400で死んでいたのを発見（Gemini単独運用で表面化せず）。
  TEMP_DEPRECATED_RE=/(opus-4-[78]|sonnet-5|fable-5|mythos-5)/で該当時はtemperature送らない。旧モデルのみ0付与。修正後Claude result:OK確認。
- **壁記号タイルの二人読み多数決を実装（adf99c4）**: buildWallCodeReadPlan/providerReadCount新設。WALL_CODE_READS_GEMINI(3)/CLAUDE(3)で
  Gemini×Gn+Claude×Cnの計(Gn+Cn)票をvoteWallCodeRunsへ。analyzeSingleにoptions.provider追加（未指定=従来のAI_PROVIDER追従・後方互換）。
  test-wallcode-vote 45✅。両AI一致記号は強く採用・片方の幻覚は落選・片AI全滅runは分母除外でもう片AIの票で成立。
- **実測結果（Gタイプ壁PB・正解87.2枚）: Gemini単独90枚(+3%)✅≫dual二人読み121枚(+39%)✗≫Claude単独158枚(+81%)✗（最悪）**。
  ローカルClaude単独E2E（recordings/diagnostic/claude-read-2026-07-20.json）+ブラウザdual実測で確定。費用: Gemini¥10/回・Claude/dual¥250/回（25倍）。
- **なぜClaudeが弱いか（ログで観測）**: Claudeは壁記号を読むがwall_length_mm:nullで返し面幅に割付できず全面PB扱い→過大。
  Geminiは記号＋寸法mmセットで返せるので面割付が効く。Claudeは外形寸法も誤読(11500×5900)。dual121枚は多数決でClaudeの過剰記号が
  Geminiの正しい読みを引き上げた中間値。外部ベンチ（Koncile・裏取り済み）もスキャン画像抽出でGemini94%>GPT91%>Claude90%と方向一致。
- **結論: 本番はAI_PROVIDER=geminiに戻す**（Render・ユーザー操作）。二人読み実装は残す（将来Claudeの面割付改善時に使える）。
  ChatGPT(OpenAI)は未実装・未検証（画像は外部OCR依存でGeminiに劣る可能性・投資対効果微妙）。詳細メモリ: ai-provider-gemini-best.md
- 反省: 「業界評価が一致」と言ってソースを1件捏造（Claude text-first説は原文になかった）→ユーザー指摘で撤回。一次データ（自前実測）＞外部記事。

**本番Gemini運用のmust-fix 2件（2026-07-17・最終監査→coder→差し戻し再レビュー承認）**:
- **タイル部分失敗の顕在化+sticky解消**: analyzeTilesが{results, failedTiles, totalTiles}を返し（API障害と「記号なし」を区別。
  Claude経路も対応）、部分失敗時は `_wall_codes_partial` + _warnings「壁記号の読取N件失敗・過大の可能性・再アップロードで再読取」。
  再読取条件に partial を追加（旧: 一度部分保存されると恒久固定→壁PB155枚級の暴走が警告なしで起きる穴だった）。モック検証18✅
- **リトライ暴走の抑制**: 日次上限の429（message内PerDay）は待たずに即断念、待機を15/30/45/60秒（上限150秒/呼・旧300秒）。
  should-fix繰越: PerDay判定のerrorDetails補強・パース失敗タイルのfailed計上・mergeDoorScheduleのnull上書き・
  summary書き戻し統一（Gemini記録でdump-ui✗になる）・実測時の壁面積カードのラベル・.env.exampleへの新環境変数記載

**資料の完全読み（2026-07-17夜・「Excel全部+図面を読んでこい」指示で実施。図面全域4倍精読+Excel全17+7シート数式ダンプ）**:
- **遮音壁の正体が確定（従来の「UB裏面が展開図外」説明は誤り→訂正）**: タイプ別シートの列=面（D/E=Ａ,G/H=Ｂ,J/K=Ｃ,M/N=Ｄ）から、
  遮音壁＝**「LDK↔洋室(1)間の壁1.45m」と「LDK↔洋室(3)間の壁1.05m」の2枚だけ**（台所Ｃ面1.45=洋1Ａ面の裏表・台所Ｄ面1.1≒洋3Ａ面1.05。
  PBは両面計上で12.9785・GWは壁1枚1回で6.425）。**両方とも展開図に写っている＝読み取り可能**。平面図の楕円記号はL14が洋1に1個あるだけで、
  洋3側には記号なし（図面は凡例の「遮音壁の範囲」マークと注意書き「遮音壁はボードをスラブ下まで+GW24kg充填」で指定）。
  エンジン対応: LDK隣接の間仕切のうち幅1.45/1.05にマッチする面を遮音扱いにすれば⏳12.98を数式で再現できる
- **下地高ルール確定（CH+370説は誤り）**: 下地高=**現場定数2.57m固定**（標準スラブ間）。**水回りのスラブ下がり(-200)部に立つ壁のみ+0.2=2.77**
  （2.77の9セルは全て水回り。居室CH2400の下地も2.57＝CH連動しない。STUD_PLENUM_M=370はCH2.2で偶然一致していただけ）。
  界壁面（木胴縁）だけ高さ2.45。PBがスラブ下まで走る壁が3箇所ある（台所Ｃ面0.85×2.77等）
- **KP枚数換算はXLSは「㎡÷2.0」**（X64注記・3.925→2.5枚/戸）。エンジンのKP_SHEET_SQM=2.2022と基準が違う（要整合判断）
- **一部界壁の数式化が可能**: 界壁面=洋3Ｃ面0.98×2.45+台所Ｃ面1.08×2.45=5.047㎡/戸÷1.5=3.36枚（推定3枚の置換先）
- **「10戸」の正体**: 現場名等シート=9戸+「造作構造材」1式のダミースロット合算。さらに**A1/B1/C1/D1の1戸派生タイプが実在**
  （67戸=(9+1)×4+9×3。やまがみ家具/木工事行で分割確認。「〜Aタイプ」見積のゴールに影響）
- **見積スコープの重要事実（やまがみ木工事の備考）**: ①**防露壁(D14)は全てGL工法・LGSボード明細で別計上**＝木工事見積に含まれない
  ②PS点検口(XLS2ヶ所/戸)は見積除外 ③**収納RC面はコンパネ→石膏ボードへVE変更(250121)**＝図面のD64は最終仕様でない
  ④EV廻りGW220→140VE減 ⑤フローリング3192㎡はVE削除(241225)でパネ協スコープ外
- その他: 際根太18.2=玄関5.7+便所4.0+洗面8.5、巾木内訳（玄関4.8+洋1 10.5+洋2 11.45+洋3 8.9+台所20.79・玄関樹脂3.75は振替）、
  ボード出隅22.02m/戸、材積係数実測（軸組0.0116m³/㎡等）、面木・タイル見切は集計表に全タイプ戸当値が直接入力済み（B〜Jシート空でも他タイプ資料あり）、
  家具及棚板内訳・個別明細シートは旧残骸で使用不可、糊釘費のボード面積4405㎡=65.8㎡/戸。
  全シートの数式付きダンプはscratchpad/dump/に保存（kizai__*/yamagami__*.txt）
- **住戸内間仕切壁はW65**（特記無き限り・注意書き）＝寸法マッチ許容差の物理根拠

**計算ロジック総監査（2026-07-17深夜・「意地でも探せ」で全式・全定数を真ルールと突合）**:
- **修正済み（must-fix A-1）**: applyElevationTakeoffの巾木マッチャ部分一致が「木製巾木出隅役物」を
  巾木のm数で上書き（11箇所→61箇所・+510%）→ 完全一致化で修正。表示スコープ外のため画面では未発覚だった
- **確定ミス（未修正・次サイクル）**:
  A-2 下地高CH+370は両方向誤り（居室2.77→正2.57で+3.79㎡過大・水回り2.57→正2.77で-1.61㎡過少・正味+2.18㎡の相殺で-5.3%に見えている。
  **高さだけ直すと-7.9%に悪化**＝真の不足6.6㎡が別在（S6便所ブロックの開口無控除疑い等）→セット修正必須。玄関・キッチンは誤式が偶然正解値）
  A-3 遮音壁・GWの高さCH+40→正2.57 / A-4 EV廻り÷1.4→正1.5 / A-5 KP換算2.2022→XLSは÷2.0（4.0〜4.4㎡帯で1枚ズレ）
  A-6 材積の片引き部0.0231・袖壁0.0106が未計上（+0.085m³/戸=間仕切木軸-12.6%の主因）/ A-9 軸組8.89→8.59m/㎡
  A-7 木胴縁係数0.00297 vs 実績0.0098m³/㎡（3.3倍差・エンジンは広面積×薄係数の逆モデル・⏳-46%の正体）
  A-8 天井式はGのﾊﾟｳﾀﾞｰﾄｲﾚ4.711㎡でのみXLSと偶然一致（XLS実式=総㎡÷1.45+7枚/9戸。他タイプで約-0.9枚/戸ズレ）
- **疑わしい（S1〜S11）**: S1 KP面G16のPBと一部界壁3枚の二重計上疑い（壁PB+7.2%の有力因・XLS台所ブロック要確認）/
  S2 展開図なしパスのリノベ係数0.6が新築Gで-36% / S3 GW推定+134%（実測上書きで隠蔽）/ S5 置床+7.6%と合板係数1.05の相殺 /
  S6 間仕切下地の開口控除が部屋で違う疑い（便所5.7×2.77無控除）/ S7 巾木の掃出し窓閾値1800 / S11 GW両面可視の二重計上（潜在）
- **丸め**: エンジンは戸単位ceil（壁+0.51枚/戸等）・XLSは総量小数保持→タイプ見積では㎡保持×戸数後に丸めへ変更推奨
- 正しさ確認済み: CH+40（壁・耐水）・KP帯ch-0.85・換算1.4/1.45/1.5適用・開口実寸控除・際根太断面・鏡像設計・C04/D64振り分け等

**割付精度サイクルA（2026-07-18・8角度コードレビュー→検証確定4件をcoder/reviewer分離で修正・承認済み）**:
全diff（3858b57..HEAD）の多角レビューで25候補→独立検証16件生存（詳細はこの節と総監査節）。うち割付系4件を修正:
- **等幅対面タイ+二重除外**（buildupCalculator placementByFace）: 第0パス新設（面のwall_codeと同記号±80mmのplacementを面ごと最近傍1件消費=二重除外防止）+タイ解決を加点式に（C下地限定: ドアある面+1/窓ある面−1/**窓+ドア同居は相殺0=読取ノイズ疑いに賭けない**。Gemini記録で悪化する実例に基づく設計）。リプレイClaude記録 壁PB93→92枚(+6%・正解87.2に接近。洋室(1)C04がドア2枚のB面→無開口D面へ=XLSと一致）
- **タイルdedupの等幅潰し**（claudeApi aggregateWallCodeItems純関数化）: `_tile`番号を付与し「同一タイル内の寸法ラベル**完全一致**件数の最大値=実在本数（上限2=対面想定）」。僅差（再転記ノイズ）は1件扱い。_tileは保存データに漏らさない。旧経路（_tileなし）は1件統合の従来互換
- **収納D6*控除の部屋対応化**: 控除プールを「平面図部屋一覧に対応の無い展開図部屋（クロゼット内RC面=合算立面）」のみに限定。平面図のみの収納（物入等）のゼロ化を修正
- **isWindowのsymbol対応**: AW/AWD・W-数字符号を窓判定に追加（WD-は非該当を確認）
- 検証: test-buildup-placement.mjs新設21✅/eval 17✅⏳3✗0/opening-match 37✅/リプレイ両記録✅8✗0。**未検証**: 実AI E2Eでの_tile効果（対面C04の2枚保持が実読みで出るか・壁PB下振れ監視。replayは集約後データのため通らない）
- レビュー残（未対応・低優先）: 同一壁ジッタ2クラスタの残存二重除外（発生条件狭）・平面図読み落とし収納の誤控除（旧バグの縮小版）
- **検証済みバグの残り**: 0.37二重定義=A-2とセット（並行セッション予約）・摘要mm併記で旧カスタム単価が外れる（データ移行の判断が必要・要ユーザー確認）

**建具マッチング層サイクルB（2026-07-19・coder/reviewer分離・差し戻し1往復・最終承認済み）**: レビュー確定4件+派生should-fix2件を修正:
- **buildDoorLookupのnull毒化解消**: 矛盾判定をフィールド単位に（両行非nullで食い違う場合のみ毒化・片方nullは寸法を持つ行を採用・name/locationも欠落補完・毒化後の復活なし）。同符号の一覧行+姿図欄再掲（寸法null）で符号マッチが死ぬバグの修正
- **mergeDoorScheduleの正規化キー化+フィールド単位補完+矛盾null化**: キーをnormalizeDoorSymbolに統一（全角/ハイフンゆれの重複エントリ解消）。put()は「prev側nullのみ埋める」ループ（丸ごとspreadでの転記値消失を修正）。非null同士の寸法矛盾は該当フィールドのみnull化+警告（field:'door_schedule_conflict'・再マージでも警告は残す・summaryにdoor_conflicts）。conflicted Mapで呼び出し内の復活防止・再アップロードによる収束経路は維持
- **玄関の推定マッチ除外をroom名でも判定**（type表記ゆれ'片開き戸'の素通り対策。符号マッチは可）
- **attachElevationDataの部屋名突合をnormalizeRoomName適用後比較に**（全角（１）ゆれでplan_codes/placementsが丸ごと落ちる残存経路を閉塞。空文字includes暴発ガード付き）
- 検証: test-opening-match 39→60✅/placement 21✅/eval 17✅⏳3✗0/replay3記録✅8✗0（数値不変は非発動の構造的必然をスクリプトで裏取り）。**実AI E2E未検証**（次回e2e-geminiで建具マージ件数・door_conflicts・plan_codes付与部屋数を確認）
- 備考: 一括upload側は建具表を生保存（マージ層を通らない）が、buildDoorLookupのフィールド単位合成が安全側でカバー

**配線・運用系サイクルC（2026-07-19・coder/reviewer分離・差し戻し1往復・承認済み）**: レビュー確定5件+差し戻し1件を修正:
- **計算警告のフロント配線**: stores/project.jsのcalculateMaterialsが/calculateレスポンスのwarnings（マージ済み最新一覧）でaiReading._warningsを置換（refごと差し替え）。木胴縁50%未満警告等が通常フロー（アップロード→計算→結果画面）で表示されるように
- **lost update窓の縮小**: /calculateは警告書き込み直前にaiReadingを再読取し最新parsedDataへ警告のみマージ（matched_by等の計算副作用はインメモリ限りになり永続化されない=parsedData汚染の解消）。/auxはmergeAuxIntoFresh（再読取版を土台に自分の変更フィールドだけ移植・_warningsは(field,message)キーの3方向マージ）。行ロックなしのms級競合窓は残存（コメント明記）
- **aux恒久エラーの文言分岐**: auxAiErrorResponse新設。キー未設定（'is not configured'）/401/403/**Gemini無効キー（HTTP 400+API key not valid）**→「運営者にご連絡ください」系、429/529/接続断→従来の再試行誘導。全て503でフロント互換維持
- **同一警告時のDB書き戻しスキップ**（JSON文字列比較）・**単価ゆるい一致にunit一致条件**（m単価×m³行の誤適用防止。完全一致側は@@unique(materialName,spec)により不要と検証済み）
- 検証: test-calc-wiring.mjs新設19✅（実ルーターマウント+スタブprisma）・eval/opening-match/placement/replay全回帰ゼロ・vite build成功
- 繰越（低優先）: mergeDoorScheduleの矛盾警告が再アップロード復活値と乖離しうる（警告空振りのみ・安全側）・fresh再読取失敗時のwarningsが古い一覧に劣化（次回calculateで自己回復）・429 PerDayの文言

**設計決定（実装前・変更可）**:
- 平面詳細図=必須、展開図・建具表=任意（無ければ従来ロジック、有れば精度モード。後方互換）
- AI呼び出し: 平面図=デュアルAI維持、展開図・建具表=Claude単体（表の転記はブレ小・コスト抑制）
- parsedDataに `elevations`（部屋ごとA〜D面幅mm・天井高・巾木種別・開口）と `door_schedule`（符号・名称・W×H・取付位置）を添付
- 計算（展開図あり時）: 壁面積=Σ(部屋周長×天井高)−開口面積、巾木=Σ周長−開口幅（木製/ソフト/樹脂別）
  ※プロの拾い出しXLS（Ａタイプシート）と同じ「部屋別A〜D面」方式なので正解データと直接照合可能
- 建具表あり時: 開口サイズは実寸（平面図の建具符号とマッチ）、建具数量も表から
- UI: Homeに3アップロードスロット → 2026-07-15にSTEP1〜3の段階式ウィザードへ変更（上記）

**プロのXLS計算ロジック（数式解読済み・2026-07-10）**:
- タイプ別シート: 部屋（7ブロック）×部位（約50行）×A〜D面の「幅×高さ」入力 → P列=1戸小計 → ×戸数
- 集計表: 部位×タイプのマトリクス。W=全タイプ総量 → ÷換算係数（壁PB 1.4㎡/枚）= 発注数量
- 造作材集計: 材積(m³) = 断面H(mm)×D(mm)×L(mm)×本数×10⁻⁹。本数=戸当×戸数。
  見切り類は断面1000×1000と入力して結果をm化する仕掛け。材種番号（1=米栂1等,3=玄関枠廻り縁等）で列振り分け
- ZAIRYOの新エンジンはこの「部屋×面×部位」ボトムアップ方式を踏襲する（XLSと行単位で照合可能になる）

**壁仕上記号（平面詳細図の各壁面に楕円で記載。凡例は図面右欄）**:
- 3桁 = 下地(英字)+中間(数字)+表面(数字)。例: D14 = RC面木(LGS)下地+PB9.5+洋室クロス貼
- 下地: Z=ナシ / C=コンクリート打放 / D=RC面木(LGS) / G=間仕切木(LGS) / H=発泡ウレタン吹付 /
  I=発泡ウレタン吹付GL下地 / L=間仕切木(LGS)+GW24K充填 / S=発泡ウレタン+木(LGS)+GW24K /
  O=間仕切木(LGS)+遮音シート張り / W=間仕切木(LGS)+GW(ボード共スラブ下まで)
- 中間: 0=ナシ / 1=PB9.5 / 2=耐水PB9.5 / 3=ラワンベニヤ2.5 / 4=PB9.5GL工法 / 5=耐水PB9.5GL工法 / 6=コンパネ下地貼
- 表面: 0=ナシ / 1=ラワンベニヤ2.5 / 2=押入ボード / 3=シナベニヤ3 / 4=洋室クロス貼 / 5=和室クロス貼 / 6=キッチンパネル貼
- 部位への振り分け: L/O/W下地=遮音壁PB張りt9.5+GW系、D=防露壁面/RC面木胴縁系、I/H=GL・ウレタン系、
  中間2/5=耐水PB、表面6=キッチンパネル。これで「線の色による躯体/間仕切判定」は不要になる

**参照図面（Aタイプの例・ユーザーPCの Pictures/zairyoの資料/意匠図_pages/）**:
- 展開図(1)Aタイプ = page_50.png（A-15-1）。読み方: 各室4面（A〜D）、下の数字=壁幅mm、左=高さ（全高2,810/CH2,400居室・2,200水回り玄関）。左端に室仕上表（床/巾木/壁/天井/備考）
- Aタイプ実測例: LDK周長21.0m(3,840+6,660×2+3,840)、玄関・廊下12.03m（A面1,385=玄関ドア・H2,200）、洋室(1)15.43m
- 建具表(1)=page_60.png（A-17-1・共用部SD）、建具表(2)=page_61.png（A-17-2・住戸玄関SD-101A 850×1,900、サッシAWD-101〜108、LDK4枚引違窓4,120×2,000）。木製建具表(WD-*)=page_63（page_62はアルミ建具表AW/AD）
- claudeApi.js構造: SYSTEM_PROMPT(L11〜)、analyzeWithGemini(L256)、analyzeWithClaude(L286)、parseJsonResponse(L349)、analyzeDrawing(L371)

## 技術スタック

- **フロントエンド**: Vue.js 3 + Vite + Tailwind CSS + Pinia（Vercelデプロイ）
- **バックエンド**: Node.js + Express + Prisma ORM（Renderデプロイ）
- **AI解析**: Google Gemini API (gemini-2.5-flash) + Claude API (claude-opus-4-8) - デュアルAI照合
- **データベース**: PostgreSQL（Render管理。※SQLiteから移行済み。再デプロイでもデータは消えない）
- **認証**: JWT（会社アカウント単位）+ ゲスト利用可
- **Excel出力**: ExcelJS

## AI解析アーキテクチャ（重要）

「AIは転記のみ、計算と検証はサーバー」が設計原則。

```
図面アップロード
  → Gemini / Claude が並行解析（転記優先プロンプト）
  → reconcileDualResults: フィールド単位で照合（平均化はしない）
  → validateAndNormalize: サーバー側検証を強制
      - 部屋面積の優先順位: ㎡ラベル転記 > 帖数×1.65 > AI目測
      - 専有面積: ユーザー入力 > AI転記ラベル（部屋合計と整合する場合） > 外形寸法アンカー > 部屋合計照合
      - AI総面積と外形寸法が矛盾したら部屋面積合計で裁定（総面積が部屋合計の0.95〜1.4倍なら
        ラベル転記とみなし外形寸法を棄却。外形寸法の誤読がラベル値を上書きする事故の防止）
      - 外形寸法がユーザー入力の専有面積と矛盾（グロスが0.95〜1.3倍の範囲外）なら棄却
      - 数量計算は専有面積を基礎面積に使う（部屋合計が専有面積×0.96未満なら不足分を居室床に補填）
      - 間仕切壁は間取りタイプ別バンドでクランプ、床暖房はLDK×70%上限
      - 図面種別ゲート: floor_plan以外は400で拒否（単一AI時も有効）
  → _warnings / _ai_disagreements を結果に添付（要確認項目）
  → materialCalculator が数量計算（validator検証済みなら二重補正しない）
```

- プロンプト例の数値コピー（捏造）は禁止指示済み。記載がないフィールドはnull
- rawResponse にAI生テキストを保存（eval・デバッグの一次資料）
- 関連ファイル: `services/claudeApi.js`（プロンプト+API呼び出し）、`services/aiReadingValidator.js`（検証・照合）、`services/materialCalculator.js`（数量計算 約2,300行）

## 認証・データ分離の仕様

| 利用形態 | 機能 | 履歴 |
|---------|------|------|
| ゲスト（未ログイン） | アップロード→見積→Excel すべて可 | 保存されない（一覧APIが空を返す） |
| ログイン（会社アカウント） | 同上 | 会社ごとに分離して保存・履歴画面で再表示可 |

- 登録は `REGISTRATION_CODE` 設定時、招待コード必須
- プロジェクトは companyId でスコープされ、他社のデータは404
- 運営者用admin API: `GET /api/admin/companies`（会社一覧+利用状況）、`POST /api/admin/companies/:id/reset-password`。`ADMIN_TOKEN` 未設定時は404で無効化、使用時は `X-Admin-Token` ヘッダ
- 運営者用管理画面: フロントの `/admin`（ナビ非表示の隠しルート）。画面内でADMIN_TOKENを入力しsessionStorageに保持

## ディレクトリ構成

```
zairyo/
├── backend-node/
│   ├── src/
│   │   ├── config.js             # JWT_SECRET等の一元管理（本番で未設定なら起動拒否）
│   │   ├── middleware/auth.js    # optionalAuth / requireAuth / projectScope
│   │   ├── routes/
│   │   │   ├── auth.js           # 会社登録・ログイン（招待コード対応）
│   │   │   ├── admin.js          # 運営者用API（ADMIN_TOKEN）
│   │   │   ├── projects.js       # プロジェクトCRUD+解析+計算+Excel
│   │   │   ├── packages.js / unitPrices.js
│   │   ├── services/
│   │   │   ├── claudeApi.js          # デュアルAI解析
│   │   │   ├── aiReadingValidator.js # サーバー側検証・照合
│   │   │   └── materialCalculator.js # 資材計算ロジック
│   ├── scripts/                  # evalスクリプト+正解データ+評価結果
│   ├── prisma/                   # schema.prisma（postgresql）+ seed.js（upsert方式・冪等）
├── frontend/
│   ├── src/
│   │   ├── router.js             # / , /login , /history , /result , /settings/prices , /settings/account , /admin
│   │   ├── stores/               # project.js , auth.js（トークン+localStorage）
│   │   ├── services/api.js       # axiosインターセプター（Bearer自動付与）
│   │   └── views/                # Home / Login / MaterialResult / ProjectHistory / UnitPriceSettings / AccountSettings / AdminDashboard
└── CLAUDE.md
```

## 全体フロー

1. **図面アップロード（Home）** - 現場名+専有面積(任意)を入力しPDF/PNG/JPGをアップロード → デュアルAI解析
2. **資材リスト（MaterialResult）** - 計算結果を表示、Excel出力。AI読み取りの要確認項目（_warnings/_ai_disagreements）を警告パネルで表示。間仕切壁延長・天井高を上書きして再計算、数量・単価の手動編集（PUT /materials）も可能
3. **履歴（ProjectHistory・要ログイン）** - 現場名で過去の見積を再表示・Excel再出力

## デザインガイドライン

- **テーマ**: ダークテーマ
- **アクセントカラー**: ゴールド (#D4A853)
- **フォント**: Noto Sans JP + DM Mono

## APIエンドポイント

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| POST | /api/auth/register | 招待コード | 会社登録 |
| POST | /api/auth/login | - | ログイン（JWT発行） |
| POST | /api/auth/change-password | 要ログイン | パスワード変更（現パスワード確認あり） |
| GET | /api/projects | 任意 | 一覧（未ログインは常に空） |
| POST | /api/projects | 任意 | 新規作成（ログイン時は会社に紐付け） |
| DELETE | /api/projects/{id} | 任意 | プロジェクト削除（関連データ+アップロードファイルも削除） |
| POST | /api/projects/{id}/upload | 任意+UPLOAD_GUARD_TOKEN | 図面アップロード+AI解析（total_area_sqm任意） |
| POST | /api/projects/{id}/aux | 任意+UPLOAD_GUARD_TOKEN | 補助図面の段階式アップロード（elevation/door_scheduleどちらか1枚。建具表は符号マージで複数ページ可） |
| PUT | /api/projects/{id}/materials | 任意 | 数量・単価編集+独自行追加（added配列、materialListId必須） |
| POST | /api/projects/{id}/overrides | 任意 | 仕様変更保存 |
| POST | /api/projects/{id}/calculate | 任意 | 資材計算実行 |
| GET | /api/projects/{id}/materials | 任意 | 資材リスト取得 |
| GET | /api/projects/{id}/export | 任意 | Excelダウンロード |
| GET | /api/unit-prices/effective | 要ログイン | 実効単価一覧（標準+自社カスタムのマージ） |
| PUT | /api/unit-prices/upsert | 要ログイン | 資材名+規格で自社単価を登録/更新 |
| POST | /api/unit-prices/reset | 要ログイン | 自社単価を全削除（=標準単価に戻す） |
| GET | /api/admin/companies | X-Admin-Token | 会社一覧+利用状況 |
| POST | /api/admin/companies/{id}/reset-password | X-Admin-Token | パスワードリセット |

※「任意」= トークンがあれば会社スコープ、なければゲストスコープ。他社のプロジェクトは404。
※ ゲストのプロジェクト所有権: POST /api/projects が `guestToken` を発行し、以降のプロジェクト系APIは `X-Guest-Token` ヘッダの一致が必要（未提示は404）。ゲスト同士でも他人のプロジェクトには触れない。
※ PUT /materials は編集元の `materialListId` をbodyに必須（並行する再計算との競合を409で検出）。
※ PUT /api/unit-prices/bulk（要ログイン）: 単価の一括保存。`{prices: [{materialName, spec, category, unit, unitPrice|null}]}`（null=カスタム解除）を1トランザクションで適用。

## 計算ロジック（実績データに基づく）

※「54ファイル」は資料のファイル数であり54現場分のデータではない。タイプ別の詳細拾い出し正解はGタイプのみ存在（scripts/内の正解JSON参照）

**⚠ 原資料の罠（2026-07-08確認）**: 拾い出し原本 `②(仮称)アルファステイツ新宮町（木及び_建材）R6,09,19.XLS`（ユーザーPCの Pictures/zairyoの資料/）の「Ａタイプ」シートは、**中身がGタイプのデータ**（シート内のタイプ欄=G・戸数9・数値がG実績と完全一致。壁PB 122.06㎡/戸・フローリング48.42㎡/戸・巾木56.44m/戸・際根太18.2m/戸・間仕切下地84.08m/戸）。Ｂ〜Ｊタイプのシートは未記入。scripts/のGタイプ正解JSONの原本はこのシート。
図面の対応: Gタイプ平面詳細図=`意匠図_split/page_045.pdf`、Aタイプ=`page_039.pdf`（平面詳細図はpage_039からタイプ順）。Aタイプの壁・天井の拾い出し正解は存在しない（atype_ground_truth.jsonは部屋面積等のみ）。

AIは図面読み取りのみ。資材数量の計算はNode.js側で実行。

### 実績データサマリー（けいとさんの資料より）

| 項目 | 実績範囲 | 固定/変動 | 備考 |
|------|----------|-----------|------|
| PB 12.5mm | 8〜60枚 | 変動 | 壁面積による |
| PB 9.5mm | 8〜40枚 | 変動 | 天井面積による |
| Mクロス | 2〜7枚 | 変動 | 水回り面積による |
| 垂木 | 10〜30束 | 変動 | 間取りによる |
| ラワンベニヤ | 4〜19枚 | 変動 | 水回り+床下地 |
| 天井クロス | 52〜75㎡ | 変動 | 天井面積 |
| 壁クロス | 187〜270㎡ | 変動 | 壁面積 |
| 巾木 | 10〜40m | 変動 | 壁延長−開口部 |
| フローリング | 50〜70㎡ | 変動 | 居室床面積 |

### 主要計算式

| 資材 | 計算式 | ロス率 |
|-----|-------|-------|
| PB 12.5mm | 壁面積(㎡) ÷ 1.6562 × 1.05 | +5% |
| PB 9.5mm | 天井面積(㎡) ÷ 1.6562 × 1.05 | +5% |
| Mクロス | 水回り壁面積 ÷ 1.6562 × 1.05 | +5% |
| 垂木 | (間仕切壁延長÷0.303×2 + 天井面積÷0.303) ÷ 12 | — |
| フローリング | 居室床面積(㎡) × 1.1 | +10% |
| 巾木 | 壁延長(m) − 開口部幅合計 | — |
| ラワンベニヤ | 水回り床面積 + 床暖房下地 + 下地更新 | — |
| 天井クロス | 天井面積(㎡) | — |
| 壁クロス | 壁面積(㎡) | — |
| 建具 | 図面から自動カウント（1LDK=7枚, 2LDK=10枚, 3LDK=15枚） | — |

### 壁面積計算

```
壁面積 = (間仕切壁延長 × 天井高 × 2) + (躯体壁延長 × 天井高 × 1) − 開口部面積
開口部面積 = ドア数 × (0.8m × 2.0m) + 窓数 × (1.5m × 1.2m)
※ 躯体壁処理: GL工法=片面(係数1)
```

### 間仕切壁延長の目安

- 1LDK（40〜50㎡）: 12〜18m
- 2LDK（50〜65㎡）: 15〜25m
- 3LDK（65〜80㎡）: 20〜30m
※ 30mを超える場合は躯体壁を含めている可能性が高い

## AI解析プロンプト

### 図面の線種ルール（実際の図面凡例に基づく）

■ 躯体壁（外周壁・RC壁）= 間仕切壁に含めない！
  - 濃紺または黒で塗りつぶされた太い壁
  - 図面の最も外側（バルコニー側、窓側、玄関側）
  - PS（パイプスペース）、MB（メーターボックス）も同様に塗りつぶし

■ 間仕切壁（LGS壁）= これだけをpartition_wall_length_mに含める！
  - 青色または水色の細い線で描かれた壁
  - 室内を仕切る壁（部屋と部屋の間）
  - 躯体壁より明らかに薄い

### 期待するJSON出力形式（現行スキーマ）

実際のプロンプトは `services/claudeApi.js` の SYSTEM_PROMPT が正。主要フィールド:

```json
{
  "document_type": "floor_plan",
  "is_analyzable": true,
  "property_name": "物件名",
  "layout_type": "3LDK",
  "outer_dimensions_mm": { "width": 9400, "depth": 7450 },
  "total_floor_area_sqm": 67.3,
  "partition_wall_length_m": 24,
  "ceiling_height_mm": 2400,
  "rooms": [
    {
      "name": "リビング・ダイニング",
      "area_jou": "10.3",
      "area_sqm_label": 16.75,
      "area_sqm": 16.8,
      "floor_type": "flooring"
    }
  ],
  "openings": [
    { "type": "片開き戸", "width_mm": 800, "height_mm": 2080, "room": "LDK" }
  ],
  "equipment": { "ub_size": "1418", "kitchen": "I型 2250", "washstand": "W1000" },
  "storage": [ { "name": "WIC", "width_mm": 1650, "has_makuradana": true, "has_hanger_pipe": true } ],
  "special": [ { "type": "床暖房", "room": "LDK", "area_sqm": 5.0 } ]
}
```

重要ルール:
- 帖数(area_jou)・㎡ラベル(area_sqm_label)・寸法は図面の記載を**転記**。記載がなければnull（推測・例のコピー禁止）
- area_sqmの最終値はサーバー側validatorが決定（㎡ラベル > 帖×1.65 > 目測）
- document_typeがfloor_plan以外なら解析は400で拒否される

## 実績データサマリー（けいとさんの資料より）

| 項目 | 実績範囲 | 固定/変動 | 備考 |
|------|----------|-----------|------|
| PB 12.5mm | 8〜60枚 | 変動 | 壁面積による |
| PB 9.5mm | 8〜40枚 | 変動 | 天井面積による |
| Mクロス | 2〜7枚 | 変動 | 水回り面積による |
| 垂木 | 10〜30束 | 変動 | 間取りによる |
| ラワンベニヤ | 4〜19枚 | 変動 | 水回り+床下地 |
| 天井クロス | 52〜75㎡ | 変動 | 天井面積 |
| 壁クロス | 187〜270㎡ | 変動 | 壁面積 |
| 巾木 | 10〜40m | 変動 | 壁延長−開口部 |
| フローリング | 50〜70㎡ | 変動 | 居室床面積 |

### 主要計算式

| 資材 | 計算式 | ロス率 |
|-----|-------|-------|
| PB 12.5mm | 壁面積(㎡) ÷ 1.6562 × 1.05 | +5% |
| PB 9.5mm | 天井面積(㎡) ÷ 1.6562 × 1.05 | +5% |
| Mクロス | 水回り壁面積 ÷ 1.6562 × 1.05 | +5% |
| 垂木 | (間仕切壁延長÷0.303×2 + 天井面積÷0.303) ÷ 12 | — |
| フローリング | 居室床面積(㎡) × 1.1 | +10% |
| 巾木 | 壁延長(m) − 開口部幅合計 | — |
| ラワンベニヤ | 水回り床面積 + 床暖房下地 + 下地更新 | — |
| 天井クロス | 天井面積(㎡) | — |
| 壁クロス | 壁面積(㎡) | — |
| 建具 | 図面から自動カウント（1LDK=7枚, 2LDK=10枚, 3LDK=15枚） | — |

### 壁面積計算

```
壁面積 = (間仕切壁延長 × 天井高 × 2) + (躯体壁延長 × 天井高 × 1) − 開口部面積
開口部面積 = ドア数 × (0.8m × 2.0m) + 窓数 × (1.5m × 1.2m)
※ 躯体壁処理: GL工法=片面(係数1)
```

### 間仕切壁延長の目安

- 1LDK（40〜50㎡）: 12〜18m
- 2LDK（50〜65㎡）: 15〜25m
- 3LDK（65〜80㎡）: 20〜30m
※ 30mを超える場合は躯体壁を含めている可能性が高い

## AI解析プロンプト

### 図面の線種ルール（実際の図面凡例に基づく）

■ 躯体壁（外周壁・RC壁）= 間仕切壁に含めない！
  - 濃紺または黒で塗りつぶされた太い壁
  - 図面の最も外側（バルコニー側、窓側、玄関側）
  - PS（パイプスペース）、MB（メーターボックス）も同様に塗りつぶし

■ 間仕切壁（LGS壁）= これだけをpartition_wall_length_mに含める！
  - 青色または水色の細い線で描かれた壁
  - 室内を仕切る壁（部屋と部屋の間）
  - 躯体壁より明らかに薄い

### 期待するJSON出力形式
```json
{
  "property_name": "物件名",
  "layout_type": "2LDK",
  "total_floor_area_sqm": 55,
  "partition_wall_length_m": 20,
  "ceiling_height_mm": 2400,
  "rooms": [
    {
      "name": "LDK",
      "area_tsubo": 8.1,
      "area_sqm": 26.8,
      "width_mm": 5000,
      "depth_mm": 5360,
      "floor_type": "flooring",
      "wall_type": "partition"
    }
  ],
  "openings": [
    { "type": "開き戸", "width_mm": 800, "height_mm": 2035, "room": "LDK" }
  ],
  "equipment": {
    "ub_size": "1216",
    "kitchen": "I型 2550",
    "washstand": "W750"
  },
  "storage": [
    { "type": "closet", "width_mm": 1800, "has_makuradana": true, "has_hanger_pipe": true }
  ],
  "special": [
    { "type": "室内窓", "details": "LDK-洋室間" },
    { "type": "床暖房", "details": "リビング", "area_sqm": 2.7 }
  ]
}
```

## 実績データ（けいとさんの資料 + アルファステイツ新宮町67戸）

### けいとさんの資料（リノベーション実績）

| 物件名 | 間取り | 合計金額 | PB12.5 | PB9.5 | Mクロス | 垂木 | 天井CL | 壁CL | 巾木 |
|-------|------|---------|--------|-------|--------|------|--------|------|------|
| 朝日パリオ305 | 2LDK | 620万 | 40枚 | 30枚 | 7枚 | 25束 | 55㎡ | 198㎡ | — |
| 別物件ミドル | 2LDK | 665万 | 50枚 | 35枚 | 7枚 | 25束 | 65㎡ | 234㎡ | — |
| 寿401 HG | 2LDK | 735万 | — | 30枚 | 7枚 | 20束 | 55㎡ | 198㎡ | — |
| 3LDK 70㎡ | 3LDK | 535万 | 35枚 | 30枚 | 7枚 | 20束 | 75㎡ | 270㎡ | 10m |
| 目白テラスドハウス3A | — | 722万 | 40枚 | 35枚 | 7枚 | 27束 | 75㎡ | 270㎡ | 15m |
| 大型物件 | — | — | 60枚 | 40枚 | 7枚 | 30束 | — | — | 40m |

### アルファステイツ新宮町（意匠図①より抽出・新築マンション67戸）

| タイプ | 専有面積 | 内法面積 | 間取り | UBサイズ | 戸数 | バルコニー | 備考 |
|--------|----------|----------|--------|----------|------|------------|------|
| A | 71.90㎡ | 68.00㎡ | 3LDK | 1416 | 10戸 | 9.46㎡ | 角部屋、LDK18.82㎡ |
| B | 67.30㎡ | 64.80㎡ | 3LDK | 1416 | 10戸 | 10.03㎡ | WIC付き |
| C | 67.30㎡ | 64.80㎡ | 3LDK | 1416 | 10戸 | 10.03㎡ | 専用階段+4.32㎡ |
| D | 67.30㎡ | 64.80㎡ | 3LDK | 1416 | 10戸 | 10.03㎡ | Cタイプ反転 |
| E | 67.31㎡ | 64.80㎡ | 3LDK | 1416 | 9戸 | 10.03㎡ | LDK18.90㎡ |
| F | 50.74㎡ | - | 2LDK | 1216 | 9戸 | 8.50㎡ | コンパクト、SIC付き |
| G | 67.30㎡ | 64.80㎡ | 3LDK | 1416 | 9戸 | 8.78㎡ | 標準タイプ |

※ UBサイズ: Fタイプのみ1216、他は全て1416

### 建具表（アルファスタイル新宮町 木製建具）

| 建具タイプ | 型番 | サイズ(W×H) | 用途 |
|------------|------|-------------|------|
| 片開き戸 | WD-1TA | 850×2175 | LDK入口 |
| 片開き戸 | WD-2A/2TA | 800×2080/2175 | 洋室入口 |
| 片開き戸 | WD-3TB | 700×2175 | トイレ・洗面 |
| 片開き戸 | WD-6A/6TA | 600×2080/2175 | 収納等 |
| 片引き戸 | WD-8A/8B | 660〜760×2075 | 洋室・水回り |
| 引違い戸 | WD-102C | 1825×2075 | LDK-洋室間 |
| 2枚折戸 | WD-12系/120系 | 605〜983×2080〜2320 | クローゼット |
| 6枚折戸 | WD-160B | 2091×2320 | WIC |

### 67戸全体実績（見積明細内訳より）

| 資材 | 67戸合計 | 1戸あたり | 単位 | 備考 |
|------|----------|-----------|------|------|
| **木材** |||||
| 際根太 LVL 30×45 | 1.8 | 0.027 | m³ | |
| 間仕切木軸 LVL 30×45 | 77.3 | 1.15 | m³ | |
| 木胴縁（界壁面） LVL 30×45 | 3.6 | 0.054 | m³ | |
| 天井下地 LVL 30×40 | 38.5 | 0.57 | m³ | |
| **建材** |||||
| 壁石膏ボード t-9.5 | 6,010 | 90 | 枚 | 3'×6' |
| 壁耐水石膏ボード t-9.5 | 280 | 4.2 | 枚 | 3'×6' |
| 一部界壁石膏ボード t-9.5 | 200 | 3.0 | 枚 | キッチンパネル下 |
| 一部界壁耐水石膏ボード t-9.5 | 50 | 0.7 | 枚 | キッチンパネル下 |
| EV廻り壁石膏ボード t-9.5 | 150 | 2.2 | 枚 | |
| 収納面石膏ボード t-9.5 | 340 | 5.1 | 枚 | MC/WIC/CLRC |
| 天井石膏ボード t-9.5 | 2,810 | 42 | 枚 | |
| 下り天井石膏ボード t-9.5 | 260 | 3.9 | 枚 | |
| キッチンパネル t-3.0 | 170 | 2.5 | 枚 | 3'×8' |
| キッチンパネル見切り | 134 | 2.0 | ヶ所 | H=2250 |
| 間仕切グラスウール t-50 | 451 | 6.7 | m² | 24kg/m³ |
| EV廻りグラスウール t-50 | 140 | 2.1 | m² | |
| 下地補強合板 t-9.0 | 390 | 5.8 | 枚 | カーテンレール・手摺下地 |
| エアコン下地補強合板 t-9.0 | 20 | 0.3 | 枚 | |
| 出隅コーナーパット | 420 | 6.3 | ヶ所 | H=2200〜2400 |
| **造作材** |||||
| 木製巾木 H=40 | 3,615 | 54 | m | LM-9KJ ホワイト |
| 木製巾木出隅役物 | 672 | 10 | ヶ所 | 4R対応 |
| 玄関見切縁 | 319 | 4.8 | m | PDD-10 |
| 天井点検口 450角 | 67 | 1 | ヶ所 | パウダールーム |
| **内装工事** |||||
| 床下地合板 T9.0 | 323 | 4.8 | m² | 水回り段差下地 |
| 乾式置床 H200 | 251 | 3.7 | m² | 水回り段差下地 |
| 床シート下合板 | 220 | 3.3 | m² | レベリング上 |

### 木製建具数量（67戸合計）

| 型番 | サイズ | 種類 | 数量 | 1戸あたり |
|------|--------|------|------|-----------|
| WD-1TA | 850×2175 | 片開き戸 | 67 | 1.0 |
| WD-2A | 800×2080 | 片開き戸 | 57 | 0.85 |
| WD-2TA | 800×2175 | 片開き戸 | 116 | 1.7 |
| WD-3TB | 700×2175 | 片開き戸 | 67 | 1.0 |
| WD-6A | 600×2080 | 片開き戸 | 19 | 0.28 |
| WD-6TA | 600×2175 | 片開き戸 | 30 | 0.45 |
| WD-6TBA | 600×2210 | 片開き戸 | 9 | 0.13 |
| WD-6C | 450×2080 | 片開き戸 | 10 | 0.15 |
| WD-8A | 660×2075 | 片引き戸 | 28 | 0.42 |
| WD-8B | 760×2075 | 片引き戸 | 48 | 0.72 |
| WD-8TB | 760×2170 | 片引き戸 | 19 | 0.28 |
| WD-102C | 1825×2075 | 引違い戸 | 9 | 0.13 |
| WD-01 | 2270×2170 | 4枚引込み戸 | 10 | 0.15 |
| WD-12AL | 605×2005 | 2枚折戸 | 9 | 0.13 |
| WD-12C | 803×2080 | 2枚折戸 | 9 | 0.13 |
| WD-12E | 983×2080 | 2枚折戸 | 9 | 0.13 |
| WD-120A | 605×2320 | 2枚折戸 | 48 | 0.72 |
| WD-120B | 701×2320 | 2枚折戸 | 10 | 0.15 |
| WD-120C | 803×2320 | 2枚折戸 | 20 | 0.30 |
| WD-120D | 905×2320 | 2枚折戸 | 9 | 0.13 |
| WD-120E | 983×2320 | 2枚折戸 | 20 | 0.30 |
| WD-160B | 2091×2320 | 6枚折戸 | 29 | 0.43 |
| **合計** | | | **652** | **9.7** |

### 1戸あたり実績（Gタイプ 65.76㎡ 基準）

| 項目 | 数量 | 算出根拠 |
|------|------|----------|
| 壁PB t-9.5 | 約90枚 | 壁面積122㎡÷1.4㎡ |
| 天井PB t-9.5 | 約42枚 | 天井面積59㎡÷1.4㎡ |
| 耐水PB t-9.5 | 約4枚 | 水回り6.5㎡ |
| キッチンパネル | 約3枚 | 3'×8' |
| 木製巾木 H=40 | 約56m | 壁延長−開口部 |
| 巾木出隅コーナー | 約10個 | |
| 床フローリング | 約48.4㎡ | 居室床面積 |
| 際根太 | 約18.2m | 床周囲 |
| 間仕切下地 | 約84m | @450ピッチ |

### UBサイズ実績

- 1216: スタンダード（1LDK〜2LDK、Fタイプ・Gタイプ）
- 1317: ミドル（2LDK）
- 1416: 中型（3LDK）
- 1418: 大型（2LDK〜3LDK、A〜Eタイプ）
- 1616: ハイグレード大型（4LDK）
- 1618: 最大サイズ（4LDK以上）

## 資材カテゴリ一覧

計算で出力される資材カテゴリ:

1. **解体工事**: 解体工事、解体廃材処分
2. **仮設工事**: 養生費
3. **左官工事**: 玄関土間左官補修、床左官補修
4. **大工工事**: 天井下地、壁下地、玄関上がり框、壁下地補強
5. **下地材**: PB 12.5mm、PB 9.5mm、Mクロス、垂木、ラワンベニヤ、ラワンランバー
6. **床材**: フローリング、床見切り、水回りフロアタイル、玄関土間タイル
7. **造作材**: 巾木、枕棚、ハンガーパイプ
8. **仕上材**: 天井クロス、壁クロス、アクセントクロス、ダイノックシート
9. **建具**: 建具一式、下駄箱
10. **設備**: UB、キッチン、洗面台、トイレ、給湯器、床暖房
11. **設備工事**: 給排水配管、UB接続、各種取付工事
12. **ガス工事**: ガス配管、コンロ繋ぎ、給湯器繋ぎ
13. **電気工事**: 分電盤、配線、ダウンライト、火災報知器
14. **電材**: 配線器具、TV端子、人感スイッチ、照明器具
15. **サッシ工事**: 網戸張替え
16. **内装材**: カーテンレール、レジスター、スリーブキャップ
17. **現場管理**: 施工管理費、現場諸経費
18. **諸経費**: ルームクリーニング、検査費

## 開発コマンド

```bash
# バックエンド（ローカルにPostgreSQLが必要。Docker or Neon無料DB）
cd backend-node
npm install
npx prisma db push      # スキーマ反映
node prisma/seed.js     # マスタ投入（upsert方式・何度実行しても安全）
npm run dev

# フロントエンド
cd frontend
npm install
npm run dev

# eval（精度検証）
node scripts/eval-gtype.js <parsedData.json> [calcResult.json]
```

## 環境変数

```env
# backend-node/.env（.env.example参照）
DATABASE_URL="postgresql://user:pass@host:5432/zairyo"
ANTHROPIC_API_KEY=xxx        # CLAUDE_API_KEYでも可（両対応）
GOOGLE_GEMINI_API_KEY=xxx
JWT_SECRET=xxx               # 本番必須（未設定だと起動拒否）
ALLOWED_ORIGINS=https://zairyo.vercel.app   # CORS制限（カンマ区切り）
UPLOAD_GUARD_TOKEN=xxx       # 任意: uploadのX-Upload-Tokenガード
UPLOAD_RATE_LIMIT=20         # 任意: uploadのIPあたり回数/時（デフォルト20）
REGISTRATION_CODE=xxx        # 任意: 登録の招待コード
ADMIN_TOKEN=xxx              # 任意: admin API有効化
PORT=8000
```

```env
# frontend（Vercel環境変数）
VITE_API_URL=https://zairyo-backend.onrender.com/api
VITE_UPLOAD_TOKEN=xxx        # UPLOAD_GUARD_TOKENと同じ値
```

## デプロイ

### フロントエンド (Vercel)
- Root Directory: `frontend` / Framework: Vite
- URL: https://zairyo.vercel.app

### バックエンド (Render)
- Root Directory: `backend-node`
- Build Command: `npm install && npx prisma generate && npx prisma db push`
- Start Command: `npm start`
- DB: Render PostgreSQL（Internal URLをDATABASE_URLに）
- seedは初回のみ: Build Commandに一時的に `&& node prisma/seed.js` を足して1回デプロイ→戻す（FreeプランはShell不可のため）

## 運用メモ

- **db push運用の注意**: 顧客の実データが入った後に破壊的なスキーマ変更をする場合は `prisma migrate` 方式へ移行すること
- **精度の前提**: 専有面積のユーザー入力を推奨（Home画面の任意欄）。±2%精度は「寸法・㎡ラベルのある平面詳細図+デュアルAI」での実測値
- **単価の適用ロジック**: 標準単価（DefaultUnitPrice・seed投入）をベースに、会社のカスタム単価（UnitPrice）を「資材名+規格の完全一致 > 規格未指定のゆるい一致」の2段階で重ねる。単価設定画面（/settings/prices）で会社ごとに編集可能。Excelインポートは標準単価と同額の行をカスタム登録しない（差分のみ保持）
- **単価コピーの移行**: 旧仕様（登録時に標準単価をコピー）で作られた既存会社のコピー行は `scripts/migrate-remove-copied-defaults.js` で1回掃除する（冪等・カスタム行は保持）
- **単価マスタの未整備**: calculatorが出力する約128資材のうち標準単価があるのは約40。残りは¥0表示になる → 実勢価格のリスト入手待ち（勝手に相場を入れない）
- **未解決の業務確認**: 既存壁PBの張り替えスコープ（全面/部分/なし）→ けいとさんへ確認中。PB枚数の妥当性はこれが決まってから
- **レートリミット**: /api/auth はIPあたり20回/15分、/upload はIPあたり20回/時（UPLOAD_RATE_LIMITで調整）、/api/admin は30回/15分。ゲストプロジェクトは24時間経過で自動削除（起動時+6時間ごと、`services/projectCleanup.js`）
- **セキュリティ対策**: helmet（セキュリティヘッダ）、アップロードのマジックバイト検証（拡張子偽装を拒否）、adminトークンはtimingSafeEqual比較、npm既知脆弱性0件（xlsxパッケージは未使用のため削除済み・Excel処理はexceljsのみ）。アップロードファイルは静的配信していない（外部から取得不可）
- **凍結タスク**: アップロード画像のS3/R2移行、会社内複数ユーザー
